# Canvas J1 / J2 — out of scope (plan定案)

## J1 · 语音 / Bridge

- **Status**: Declared out-of-scope for this repository iteration; no active gateway voice pipeline.
- **Pointer**: Keep MCP / HTTP tool surfaces documented in `docs/PLUGIN_COMPAT_MATRIX.md` if vendors add voice later.

## J2 · WebSearch / Notebook

- **Status**: Out-of-scope; continue using MCP presets and `cai-agent mcp-check --preset …` paths documented in onboarding docs.
- **Pointer**: `mcp-check` CLI and `fetch_url` permissions remain the supported web-read surfaces.
