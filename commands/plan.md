# /plan

## 用途

对目标任务生成只读计划，不直接执行工具修改。

## 上下文入口

规划开发任务前先读 `docs/NEXT_ACTIONS.zh-CN.md`。只有当该页信息不足时，再按链接读取 `DEVELOPER_TODOS.zh-CN.md`、`TEST_TODOS.zh-CN.md` 或其他主文档。

## 建议执行流程

1. 读取相关规则与技能，例如 `plan-then-execute`、`research-first`。
2. 产出结构化计划：目标与风险、目标文件清单、分步实现方案、验证与回滚策略。
3. 等待用户确认后进入执行阶段。
