## Python 异步与并发规则

> 适用范围：`asyncio`、线程池/进程池、并发 I/O 与并行任务调度。

### 1. 选择模型

- I/O 密集任务优先异步模型；
- CPU 密集任务优先进程池或外部任务系统；
- 不要为小任务过度并发化。

### 2. 一致性

- 单模块内尽量保持同步或异步风格一致；
- 避免同步函数里层层嵌套 event loop。

### 3. 可取消与超时

- 并发任务必须配置超时或取消机制；
- 被取消任务应清理资源，避免僵尸任务。

### 4. 错误处理

- 并发批处理要能汇总子任务失败信息；
- 不应吞掉异常后静默返回成功。

