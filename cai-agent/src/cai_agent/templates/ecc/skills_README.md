# skills

本目录放置 **`*.md`** 技能说明；`README.md` 自身不参与技能匹配。

- 与命令/代理绑定的技能命名尽量与 `commands/`、`agents/` 中的 id 对齐。
- 技能 hub、自进化与导出：`cai-agent skills …`、`cai-agent export --target cursor|…`。

约定索引：`cai-agent ecc -w . layout --json` → **`ecc_asset_layout_v1`**（`-w` 须写在子命令前）。
