# Common 规则示例（common/）

> 可按需新增更多文件，本文件仅作示例，规划时会被注入到 `cai-agent plan` 的 system prompt。

## 通用工程与安全规则

- 尽量保持变更范围最小化，避免一次提交跨多个不相关子系统。
- 修改配置或依赖时，必须在计划中标注潜在影响面和回滚方式。
- 涉及网络请求、外部命令或文件写入的改动，应在计划中明确：
  - 对错误和超时的处理策略；
  - 对日志与隐私的处理（避免泄露敏感信息）。

