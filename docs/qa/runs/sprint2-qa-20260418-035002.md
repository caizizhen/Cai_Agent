# Sprint 1+2 冻结日 QA 回归报告（beta 前）

> 本文由 QA 在 Sprint 2 冻结评审前产出。若 Go，则 PM 打 `v-model-switcher-beta` tag；若 No-Go，回退到 alpha。

## 0. 基本信息

- **Sprint**: S2（多供应商 + 路由）
- **分支**: `feat/model-switcher`
- **生成时间**: 2026-04-18 03:50:02 (UTC+8 本机)
- **Repo HEAD**: `4e5f26799df3e442ef872479a6d3956afe6ec832`
- **Python**: 3.13.13 / Windows 11 (10.0.26200)
- **测试策略**: 全部 mock 网络（`httpx.MockTransport` + `CAI_MOCK=1`），不消耗真实 chat token，符合 devplan §5.1 备注。

## 1. 自动化基线

| 项 | 命令 | 结果 | 备注 |
|----|------|------|------|
| pytest 全量 | `python -m pytest -q cai-agent/tests` | **100 passed** | 0.62 s；覆盖配置 / CLI / adapter / factory / 路由 / security |
| 脚本回归 | `python scripts/run_regression.py` | **PASS（27/27）** | 报告：[regression-20260418-034808.md](regression-20260418-034808.md) |

`security-scan --json` 对当前仓 `ok=True findings=0`（占位符 `api_key = "lm-studio"` 未被误报）。

## 2. Sprint 1+2 手工矩阵（15 用例）

驱动脚本：`.tmp_qa/sprint2_matrix.py`（不入仓，临时）。所有 PASS：

| # | 用例 | 覆盖 DoD | 结果 | 说明 |
|---|------|----------|------|------|
| 1 | `legacy_llm_doctor` | S1 兼容 | PASS | 只写 `[llm]` 时 `doctor` 显示 `Profile: default (共 1 个)`，无 AUTH_FAIL |
| 2 | `legacy_llm_run_mock` | S1 兼容 | PASS | `CAI_MOCK=1` 下 `cai-agent run "hello"` 返回 mock payload，rc=0 |
| 3 | `models_add_list` | S1 M3 | PASS | `add gpt+preset openai`、`add c+preset anthropic` → list 包含两条，active=gpt |
| 4 | `models_use_swap` | S1 M3 | PASS | `models use c` 后 list 的 active=c |
| 5 | `models_rm_keeps_tail` | S1 M3 | PASS | `models rm gpt` 后只剩 `c` |
| 6 | `atomic_write_bak_present` | S1 非功能 | PASS | 写入后存在 `cai-agent.toml.bak` |
| 7 | `toml_roundtrip_keeps_llm_section` | S1 兼容 | PASS | 用户原 `[llm]` 段在多次 add/use/rm 后仍保留 |
| 8 | `doctor_env_missing_banner` | S1 M6 | PASS | `api_key_env = "CAI_QA_NEVER_SET_KEY"` 未导出 → doctor 输出含 `AUTH_FAIL` 且点名 env 名 |
| 9 | `security_scan_positive_block` | S2 M13 | PASS | 真实 `sk-ant-…` → exit 2，命中 `anthropic_api_key` + `cai_profile_plaintext_api_key`（均 high） |
| 10 | `security_scan_placeholder_no_block` | S2 M13 回归 | PASS | `api_key = "lm-studio"` 占位符不降可用性，exit 0 |
| 11 | `ping_env_missing` | S1 M6 | PASS | `api_key_env` 未设置 → status=`ENV_MISSING` |
| 12 | `ping_openai_compat_200` | S1 M6 | PASS | OpenAI 兼容 `/models` 200 → status=`OK` |
| 13 | `ping_anthropic_headers` | S2 M6/M10 | PASS | URL=`…/v1/models`；`x-api-key=k`；`anthropic-version=2023-06-01` |
| 14 | `ping_401_auth_fail` | S1 M6 | PASS | 401 → status=`AUTH_FAIL` |
| 15 | `use_model_cross_provider_first_message_routes_correctly` | S2 M11/M8 | PASS | active `oai`→首条走 openai 适配器；切到 `c`→首条走 anthropic 适配器，model 正确投影 |

## 3. 供应商矩阵（devplan §5.2 / backlog §5.1.1）

| 场景 | 覆盖形式 | 结果 | 证据 |
|------|----------|------|------|
| ChatGPT 官方（`provider=openai`） | pytest `test_llm_factory_dispatch` + 矩阵 #15 | PASS | 适配器收到 `projected.base_url` 以 `/v1` 结尾 |
| Claude 原生（`provider=anthropic`） | pytest `test_llm_anthropic_adapter` + 矩阵 #13/#15 | PASS | `/v1/messages`、`system` 独立字段、`max_tokens`、`x-api-key`、`anthropic-version` 全部就位 |
| Claude via OpenRouter（`provider=openai_compatible`） | pytest `test_model_profiles_config` / `test_llm_factory_dispatch::test_openai_compatible_dispatches_to_openai` | PASS | 走 openai 适配器；base_url 带 `/v1` |
| 本地 LM Studio / Ollama | 矩阵 #2（`CAI_MOCK`），legacy `[llm]` 保底 | PASS | 无回归 |
| `/use-model` 跨 provider 切换 | 矩阵 #4/#15 | PASS | 首条消息正确路由，`Settings.from_sources` 投影完整 |
| subagent 路由 | pytest `test_factory_routing_and_security::AgentsRoutingTests` | PASS | 非 default 角色 → `chat_completion_by_role(role="subagent")` |
| planner 路由 | pytest `test_llm_factory_dispatch::test_planner_role_swaps_provider_per_call` + `test_plan_sessions_cli` | PASS | `cai-agent plan` 走 planner profile |

## 4. 安全 / 非功能

| 项 | 结果 | 说明 |
|----|------|------|
| `models add/edit/rm` 原子替换 | PASS | 矩阵 #6；中断不会产生半写文件 |
| `api_key` 明文不出现在 `session.json` / `doctor` 输出 | PASS | `doctor` 走 `_mask_api_key`；`profile_to_public_dict` 仅带 `api_key_literal=True`，不回显值 |
| Windows 下 `~/.cai-agent` 路径 | 待 Sprint 3 | 用户级回退尚未实装（backlog 列为可选 P1，未阻塞 beta） |
| 删除 active profile 回退首个 | PASS | `pick_active` + `_cmd_models` rm 分支自动兜底（矩阵 #5） |
| security-scan 对 `models.profile.api_key` 明文高危 | PASS | 矩阵 #9；规则 `cai_profile_plaintext_api_key` |
| sk-ant- / sk-or- 规则 | PASS | 矩阵 #9；新增两条 high 规则，命中并阻断 |

## 5. 已知限制（不阻断 beta）

1. **TUI 面板** 未上线：S3 才交付 M4；beta 用户按 devplan §6.2 继续走 CLI。
2. **用户级 `~/.cai-agent/models.toml` 回退** 未实装：backlog §2.3 标为可选；P2+。
3. **真实供应商冒烟** 未执行：内测侧请在本地 export `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` 后各跑一次 `cai-agent models ping <id>` + `cai-agent run "hello"`，反馈走 Issue 模板 `feat/model-switcher`。
4. **`/models refresh`（M5 的远端拉取）** 由 Sprint 3 M5 完成；当前 `models fetch` 仅本地 provider。

## 6. 缺陷

- 本轮 **0 个 P0/P1**。
- 无 P2 待修（`doctor` 已按 devplan §2.2 细化 AUTH_FAIL 展示）。

## 7. Go / No-Go

**Go — 建议按计划打 `v-model-switcher-beta` tag。**

理由：
- Sprint 1 DoD 全绿（§2.2 五条），Sprint 2 DoD 全绿（§3.2 五条）；
- 100/100 单测 + 27/27 脚本回归 + 15/15 手工矩阵 全部 PASS；
- backlog §5.1.1 六条供应商用例全部覆盖；
- 安全侧 `cai_profile_plaintext_api_key / sk-ant- / sk-or-` 规则已上线并阻断测试样本；
- 已知限制均在 Sprint 3 规划内或为非目标项。

内测沟通（devplan §6.2 beta Release Note）可以发出。

---

*本报告维护者：QA；关联文件：[MODEL_SWITCHER_BACKLOG.zh-CN.md](../../MODEL_SWITCHER_BACKLOG.zh-CN.md)、[MODEL_SWITCHER_DEVPLAN.zh-CN.md](../../MODEL_SWITCHER_DEVPLAN.zh-CN.md)。*
