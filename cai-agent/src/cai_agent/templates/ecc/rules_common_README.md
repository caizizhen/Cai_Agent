# rules/common

此目录下的 `*.md` 会在 **`cai-agent plan`** 阶段合并进系统提示（见 `cai_agent.rules`）。

- 放「跨语言 / 通用」约定（代码风格、测试门禁、安全红线）。
- 语言专属规则请放在 `rules/python/` 等子目录。

更多：`docs/CROSS_HARNESS_COMPATIBILITY.zh-CN.md`（仓库根资产目录约定）。
