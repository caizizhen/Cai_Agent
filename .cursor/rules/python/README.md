# Python 规则示例（python/）

> 可按需新增更多文件，本文件仅作示例，规划时会被注入到 `cai-agent plan` 的 system prompt。

## Python 代码约定

- 新增或修改的模块优先保持无副作用导入（避免在 import 时做 IO 或网络请求）。
- 能够加类型标注（type hints）的新函数与公共接口，应在计划中显式指出并在实现时补齐。
- 变更涉及异常处理时，应：
  - 避免无信息的裸 `except:` 捕获；
  - 明确记录错误上下文，但不要把完整堆栈或敏感配置写入长期日志。

