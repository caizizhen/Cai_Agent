## Changelog

> Version history for `cai-agent`. **This file (`CHANGELOG.md`) is the default English changelog.** For the full Chinese log see **`CHANGELOG.zh-CN.md`**. The root **`README.md`** is English by default; **`README.zh-CN.md`** is the full Chinese readme.

### Unreleased

- **SAFETY-N01-D01 unrestricted-mode config flag**: Added `[safety].unrestricted_mode` (default `false`), overridable via `CAI_UNRESTRICTED_MODE`; surfaced as `doctor_v1.unrestricted_mode` and `tool_gateway_guard_v1.policy.unrestricted_mode`; starter template includes `[safety]`. Intended precursor for future “fewer task gates + dangerous-action confirmation” behavior.

- **GW-SLASH-N01 gateway slash catalog**: Added `gateway slash-catalog --json` and `GET /v1/gateway/slash-catalog` (`gateway_slash_catalog_v1`) to expose an offline command catalog for Discord application commands, Slack `/cai` subcommands, and Teams command lists, including execute-capable command counts for operator review.

- **GW-CHAN-N01 gateway channel-monitor surface**: Added `gateway channel-monitor --json` and `GET /v1/gateway/channel-monitor` (`gateway_channel_monitor_v1`) as a compact channel monitoring view derived from `gateway_production_summary_v1`. The surface supports platform filtering and `only_errors` filtering for operator scripts without parsing the full production summary.

- **OPS-MW-N01 ops serve multi-workspace discovery**: Added `GET /v1/ops/workspaces` (`ops_workspaces_v1`) to enumerate only the server `--allow-workspace` roots, with dashboard/HTML/interactions route URLs per workspace and optional `include_summary=1` dashboard summary aggregation. OpenAPI and ops docs now expose the route.

- **OPS-RBAC-N01 ops serve RBAC and workspace-scoped audit**: `cai-agent ops serve` now accepts `--role viewer|operator|admin` as the server-side maximum role. Dashboard interaction requests may include `X-CAI-Actor` and `X-CAI-Role`; `viewer` is read/preview/audit only, `operator` may apply schedule reorder and gateway binding edits, and `admin` may also apply profile switches. RBAC denials return `rbac_forbidden` and append `ops_dashboard_action_audit_v1` rows with `actor`, `role`, and `workspace_scope`. OpenAPI discovery documents the new headers.

- **Product queue sync and next-surface execution pass**: Closed `SYNC-N01`, `MEM-N01`, `RT-N01`, `WF-N01`, and `BRW-N04`. Memory provider contracts now document and test `list/use/test` plus the `honcho_external` mock HTTP adapter; runtime docs now define mock/doctor/opt-in real smoke layers; workflow gained step `when`, `retry.max_attempts`, and `workflow_aggregate_v1`; browser tasks now map `browser_task_v1.steps[]` to explicit confirmed Playwright MCP `mcp_call_tool` calls (`browser_mcp_execution_v1`) with dry-run/refusal/audited execution states. Tests: `test_browser_provider_cli.py`, `test_browser_mcp_cli.py`, `test_cli_workflow.py`, `test_memory_provider_contract_cli.py`, and runtime mock tests.

- **BRW-N05 Browser audit and artifact manifest**: Non-dry-run browser execution attempts now append `.cai/browser/audit.jsonl` (`browser_audit_event_v1`) and refresh `.cai/browser/artifacts-manifest.json` (`browser_artifact_manifest_v1`) for refused and confirmed paths. The manifest enumerates screenshots, downloads, and traces with path, relative path, size, and mtime metadata, and the execution payload returns `browser_audit_summary_v1` plus the manifest summary. Tests cover refusal, confirmed execution, and artifact discovery.

- **Official model context-window table refreshed for hosted integrations**: `infer_default_context_window()` now uses an ordered model-prefix table before provider fallbacks, covering current official defaults for OpenAI GPT-5.5/5.4/5.2 families, Claude, Gemini, DeepSeek, GLM, Qwen, Kimi, MiniMax, Grok, Groq-hosted OSS models, Mistral, Cohere Command, and Perplexity Sonar. OpenRouter and router-style providers now strip vendor prefixes and reuse the same table. Explicit `context_window` and local/self-hosted endpoints still win/remain manual.

- **Context-window auto-inference expanded to broad integrable model families**: Added OpenRouter vendor-prefix routing and model-family rules so supported integrations infer defaults across mainstream providers (including Qwen, MiniMax, Kimi, Zhipu GLM, Mistral, Volcengine/Doubao, Meta Llama, and Perplexity) while keeping unknown or localhost/self-hosted endpoints manual.

- **Built-in third-party presets now carry official context-window defaults**: Added explicit `context_window` values for bundled hosted presets (`nous_portal`, `nvidia_nim`, `xiaomi_mimo`, `kimi_moonshot`, `minimax`, `huggingface`) so onboarding/apply flows auto-fill provider model limits out of the box. Inference fallback rules were aligned for these model IDs as well. Local/self-hosted endpoints still require manual configuration and are intentionally not auto-pinned.

- **Context window now auto-infers for legacy third-party `[llm]` configs**: `synthesize_default_profile` now applies `infer_default_context_window()` for hosted providers, so legacy single-model integrations automatically use provider/model context defaults (for example `gpt-4o` → `128000`) without manually setting `[llm].context_window`. Local/self-hosted OpenAI-compatible endpoints remain unchanged (`context_window` stays unknown unless explicitly configured). Tests updated in `test_context_usage_bar.py` and `test_model_profiles_config.py`.

- **MiMo provider preset/docs update**: Updated `xiaomi_mimo` default model to `MiMo-V2.5-Pro`, and documented the official OpenCode-style key mapping (`MIMO_API_KEY`) plus dedicated base URL override flow (`models edit --api-key-env ... --base-url ... --model ...`) in README.

- **UX-N01-D06 experience layer phase 6 (plan/workflow/release-ga failure hints convergence)**: Added standardized next-step hints for `plan` failures (`config_not_found`, `goal_empty`, `llm_error`) across JSON/text outputs; added aligned `hint:` guidance for `workflow` template/missing-file/config/runtime failure paths; and added `hints[]` to failed `release-ga` JSON payloads plus text-mode hint lines after failed checks. Tests: `test_plan_sessions_cli.py`, `test_cli_workflow.py`, `test_release_ga_cli.py`, and `test_cli_misc.py`; full `pytest` and smoke are green.

- **UX-N01-D05 experience layer phase 5 (run-family failure hints coverage)**: Extended standardized failure hints across the run-family commands (`run`/`continue`/`command`/`agent`/`fix-build`) for `config_not_found`, `goal_empty`, `command_not_found`, and `agent_not_found`. JSON failure payloads now consistently carry `hints[]`, and text mode emits aligned `hint:` lines; missing command/agent flows now point directly to `commands --json` / `agents --json`. Tests: `test_cli_misc.py` adds `command_not_found` and `agent_not_found` hint regressions; full `pytest` and smoke are green.

- **UX-N01-D04 experience layer phase 4 (run/continue failure hints)**: Standardized next-step hints across `run/continue` failure paths (`plan_file_error`, `load_session_failed`, `invalid_session`). JSON failure payloads now include `hints[]`, and text mode prints aligned `hint:` lines to stderr so troubleshooting guidance is consistent across machine/human surfaces. Added `_run_continue_failure_hints` as the single hint source. Tests: `test_cli_misc.py` adds continue failure JSON/text hint assertions; full `pytest` and smoke are green.

- **UX-N01-D03 experience layer phase 3 (sessions/continue discoverability)**: Added quickstart guidance to `sessions --help` and session-discovery hints to `continue --help`. The `sessions` text output now prints explicit next steps when there are no saved sessions and emits a ready-to-copy `cai-agent continue ...` example when sessions exist. Tests: `test_cli_misc.py` adds `sessions --help` and `continue --help` assertions; full `pytest` and smoke are green.

- **UX-N01-D02 experience layer phase 2 (failure hints + help discoverability)**: Added onboarding quickstart guidance to top-level `cai-agent --help`, and standardized missing-config failure hints for `models`/`ecc`/`doctor`/`repair`/`tools` so users always get a concrete next step (`cai-agent onboarding` → `cai-agent init --preset starter`). Added `_print_config_not_found_hint` to centralize this UX path. Tests: `test_cli_misc.py` now covers root help quickstart and doctor missing-config hints; full `pytest` and smoke are green.

- **UX-N01-D01 experience layer phase 1 (onboarding-first)**: Added `cai-agent onboarding` as a dry-run aggregation entrypoint (`onboarding_quickstart_v1`) that standardizes `init -> doctor -> models onboarding -> run/ui/sessions-recap` next steps. `init`, `doctor`, README, and ONBOARDING docs now point to onboarding-first guidance, and TUI task-board session hints now surface `/recap` for faster discoverability. Tests: onboarding CLI coverage in `test_cli_misc.py`, plus `test_doctor_cli.py`, `test_tui_session_strip.py`, full `pytest`, and smoke.

- **ECC-N02-D08 ingest gate smoke + CLI regression**: Added end-to-end smoke coverage for `skills hub install` ingest gating (safe dry-run JSON path + dangerous hooks rejection path with `ingest_gate_rejected` / exit 2), and added CLI regression test coverage in `test_skills_lint_cli.py`. This hardens the install-path contract beyond unit-level helper tests.

- **ECC-N02-D07 skills hub install ingest gate**: **`apply_skills_hub_manifest_selection`** now runs **`build_ecc_pack_ingest_gate_for_explicit_hooks_v1`** when the manifest copies **`hooks.json`** entries (**`ingest_scan_kind=explicit_hooks`**), attaches **`ingest_gate`** to **`skills_hub_pack_install_v1`**, and refuses non-**`dry_run`** writes with **`ingest_gate_rejected`**. CLI **`skills hub install`** exits **2** on failure. **`ecc_pack_ingest_gate_v1`** now carries **`ingest_scan_kind`** / **`explicit_scanned_paths`**. Tests: **`test_skills_hub_install_ingest.py`**, **`test_ecc_pack_ingest_gate`**, **`test_api_http_server`**.

- **ECC-N02-D06 README + doctor/API pack ingest visibility**: Root **`README.md` / `README.zh-CN.md`** now document **`ecc pack-import`** **`ingest_gate`**, **`ingest_gate_rejected`**, and how to read the gate from **`doctor --json`** / **`GET /v1/doctor/summary`**. **`doctor_v1`** adds **`ecc_pack_ingest_gate`** (full **`ecc_pack_ingest_gate_v1`**); **`api_doctor_summary_v1`** adds **`ecc_pack_ingest_gate`** as **`api_ecc_pack_ingest_gate_summary_v1`** (counts only); human **`doctor`** prints a short ECC ingest summary; **`doctor_upgrade_hints_v1`** recommends **`ecc pack-import --from-workspace <DIR> --json`**. RFC **`HM_02_MINIMAL_SERVER_CONTRACT.zh-CN.md`** table updated. Tests: **`test_doctor_cli`**, **`test_api_http_server`**.

- **ECC-N02-D05 pack ingest gate (hook_runtime-aligned)**: Added **`ecc_pack_ingest_gate_v1`** (`ecc_ingest_gate.build_ecc_pack_ingest_gate_v1`) scanning **`hooks.json`** under source **`rules`/`skills`/`agents`/`commands`**, classifying hook argv with the same dangerous-command substring rules as **`hook_runtime`** (**`list_hook_argv_danger_matches`** / **`hook_argv_matches_ingest_denylist`**) and blocking **`script_outside_workspace`**. **`ecc pack-import`** plans now embed **`ingest_gate`**; **`--apply`** aborts with **`ingest_gate_rejected`** when the gate fails. **`ecc pack-repair`** stays read-only (no `--apply`). Tests: **`test_ecc_pack_ingest_gate.py`**; smoke asserts **`ecc pack-import --json`** includes a passing **`ingest_gate`**.

- **CC-N04 session/recap convergence**: Added **`session_recap_v1`** via `context.build_session_recap_v1`, CLI **`sessions --recap [--json]`**, and TUI **`/recap`** for stable recent-session replay summaries (latest path, aggregate errors/tokens, replay command hints). Updated session-strip UX copy to surface `/recap` consistently. Tests: **`test_session_recap.py`**, **`test_tui_session_strip.py`**.

- **HM-N04-D01 dashboard interaction contract hardening**: `ops serve` now enforces **GET** `/v1/ops/dashboard/interactions` as preview/audit-only and returns `execute_forbidden` for `mode=apply`; write actions are routed through **POST** `/v1/ops/dashboard/interactions` (supports `mode=apply|preview|audit`) to keep mutation paths explicit and audit-aligned with `ops_dashboard_action_audit_v1`. Tests updated in **`test_ops_http_server.py`**.

- **HM-N03-D01 API status routes**: Added **`GET /v1/health`** (`api_health_v1`: version, workspace, `auth_enforced`) and **`GET /v1/ready`** (`api_ready_v1`: non-secret readiness fields from loaded `Settings`); **`GET /healthz`** / **`GET /health`** now return **`api_liveness_v1`** (still no Bearer). Updated **`docs/rfc/HM_02_MINIMAL_SERVER_CONTRACT.zh-CN.md`** and `api serve` banner. Tests: **`test_api_http_server.py`**, **`test_api_status_routes.py`**.

- **ECC-N03-D04 structured home diff**: Added **`ecc_structured_home_diff_v1`** / **`ecc_structured_home_diff_bundle_v1`** (file-level **add** / **update** / **skip** / **conflict** via SHA256 vs `export_ecc_dir_diff_v1` path sets), CLI **`cai-agent ecc home-diff [--target …] --json`**, multi-target **`ecc_structured_home_diff_multi_v1`**, **`doctor_v1.ecc_structured_home_diff`**, **`repair_plan_v1.ecc_structured_home_diff_pending_targets`** + **`ecc_home_diff_preview_commands`**, and **`doctor_upgrade_hints_v1`** entry. Tests: **`test_export_sync_diff.py`**, **`test_doctor_cli.py`**, **`test_repair_cli.py`**; smoke runs **`ecc home-diff --json`**.

- **ECC-N03-D03 harness target inventory**: Added **`ecc_harness_target_inventory_v1`** via **`build_ecc_harness_target_inventory_v1`** (`ecc_layout.py`): per-harness export root (cursor/codex/opencode), manifest/catalog flags, component file counts, plus **`workspace_sources`** for repo **`rules`/`skills`/`agents`/`commands`**. New CLI **`cai-agent ecc inventory --json`**; **`doctor_v1.ecc_harness_target_inventory`** and **`doctor_upgrade_hints_v1`** entry. Tests: **`test_ecc_layout_cli.py`**, **`test_doctor_cli.py`**; smoke runs **`ecc inventory --json`** after export in the isolated workspace.

- **ECC-N02-D04 asset pack repair diagnostics**: Added **`build_ecc_asset_pack_repair_report_v1`** (`ecc_asset_pack_repair_report_v1`) comparing **`ecc pack-manifest`** expectations to on-disk export trees (missing files, catalog hash drift, stale `cai-export-manifest.json` catalog schema, corrupt manifest JSON). New CLI **`cai-agent ecc pack-repair [--target …] --json`**, **`doctor_v1.ecc_asset_pack_repair`**, **`repair_plan_v1.ecc_pack_repair_*`**, and **`doctor_upgrade_hints_v1`** entry. Tests: **`test_asset_pack_repair.py`**, **`test_doctor_cli.py`**, **`test_repair_cli.py`**; smoke runs **`export` + `ecc pack-repair`** with an isolated workspace `cai-agent.toml`.

- **CC-N03-D04 plugins sync-home safe apply started**: `cai-agent plugins sync-home` now has an `--apply` path that emits `plugins_sync_home_result_v1`. The apply path refuses divergent existing destination directories by default, reports `conflicts[]`, and only replaces content with explicit `--force`, writing `.backup-*` directories unless `--no-backup` is supplied. Tests added in `test_plugin_compat_matrix.py`; execution is currently blocked in this sandbox because Python/uv is denied.
- **Docs**: `DEVELOPER_TODOS.zh-CN.md` and `TEST_TODOS.zh-CN.md` now list **open items only**; **Done** table rows moved to **`docs/TODOS_DONE_ARCHIVE.zh-CN.md`**; `docs/README.zh-CN.md` and `AGENTS.md` index updated.
- **CC-N03-D03 (plugins home drift)**: Added **`plugins_home_sync_drift_v1`** (same diffs as **`ecc_home_sync_drift_v1`**): **`doctor --json`** → **`plugins.home_sync_drift`**, human doctor summary lines, **`repair_plan_v1.plugins_sync_home_preview_commands`**, and **`api_doctor_summary_v1.plugins_home_sync_drift_targets`**. Tests: `test_plugin_compat_matrix.py`, `test_doctor_cli.py`, `test_repair_cli.py`, `test_api_http_server.py`; smoke asserts doctor JSON.
- **CC-N03-D02 (`plugins sync-home` dry-run)**: Added **`cai-agent plugins sync-home`**, emitting **`plugins_sync_home_plan_v1`** (same export-root conventions as **`export`/`ecc sync-home`** for rules/skills/agents/commands; **codex** is manifest-only). Extended **`doctor_upgrade_hints_v1`** and smoke now exercises **`plugins sync-home --json`**. Tests: `test_plugin_compat_matrix.py`.
- **ECC-N01 / ECC-N02 / CC-N01-D04 / HM-N01-D03 (batch closeout)**: Added **`cai-agent ecc sync-home`** (`ecc_home_sync_result_v1` / `ecc_home_sync_plan_v1`), **`cai-agent ecc pack-manifest`** (`ecc_asset_pack_manifest_v1`), and **`export --dry-run`**; extended **`export_ecc_dir_diff_v1`** to **codex/opencode**; **`doctor_v1.ecc_home_sync_drift`**, **`repair_plan_v1.ecc_sync_commands`**, **`api_doctor_summary_v1.ecc_home_sync_drift_targets`**, and **`doctor_upgrade_hints_v1`**; added **`load_agent_settings_for_workspace`** plus **`--config`** on **gateway discord/slack** execution paths. Tests: `test_ecc_layout_cli`, `test_repair_cli`, `test_doctor_cli`, `test_api_http_server`; full suite **830 passed** + smoke **NEW_FEATURE_CHECKS_OK**.
- **HM-N01-D03 (partial) HTTP API config parity with CLI**: `cai-agent api serve` now accepts **`--config`**, loads settings via **`load_settings_for_agent_api_server`**, and pins **`Settings.workspace`** to the **`-w`** root so **`/v1/profiles`** (and related routes) expose the same **`active_profile_id`** / **`profile_contract.workspace_root`** as **`--config` + `--workspace`** on the CLI. Tests: `test_api_http_server.py::test_api_profiles_use_server_config_path_and_workspace`.
- **HM-N01-D02/D04/D05 profile home operator path**: Added `cai-agent models clone`, `clone-all` (`--dry-run`, `--no-copy-home`, `--force-home`, `--set-active`), and `models alias` (`models_alias_v1`); `profiles.py` now ships `clone_profile_home_tree`, `build_models_alias_v1`, and `build_profile_home_migration_diag_v1`; `doctor --json` / `api_doctor_summary_v1` include `profile_home_migration`, and text doctor prints orphan `.cai/profiles/*` plus migration hints. Tests: `test_profile_clone_alias_cli.py`; smoke covers `models clone --dry-run --json` and `models alias --json`.
- **CC-N02-D04 feedback bundle/export redaction and dest policy**: Expanded `sanitize_feedback_text` (workspace + home path stripping, `/home/<user>` masking, Slack `xox*` tokens), sanitize `append_feedback` before persistence, re-sanitize each row in `feedback export`, hide absolute workspace in `feedback_stats` / `feedback_export_v1` / `feedback_bundle_export_v1`, add `dest_placement` plus `redaction.warnings` when `--dest` is outside the workspace, and document `feedback bundle` in the release runbook. Tests: `test_feedback_cli`, `test_feedback_export`, `test_feedback_bundle_cli`, `test_doctor_cli`.
- **ECC-N04-D03 ingest provenance / signature / trust policy draft**: Added bilingual `docs/ECC_04C_INGEST_PROVENANCE_TRUST.zh-CN.md` + `docs/ECC_04C_INGEST_PROVENANCE_TRUST.md` and `docs/schema/ecc_ingest_provenance_trust_v1.snapshot.json` (`ecc_ingest_provenance_trust_v1`) describing conservative gates that combine sanitizer output with trust levels; updated `ecc_asset_registry_v1.snapshot.json` boundaries, `docs/schema/README*.md`, `ROADMAP_EXECUTION.zh-CN.md` §10, developer/gap docs, and `cai-agent/tests/test_ecc_ingest_schema_snapshots.py` to keep draft JSON parseable.
- **English product-gap companion refresh**: Expanded `docs/PRODUCT_GAP_ANALYSIS.md` to mirror the Chinese priority table (P0–P3) and the post-`HM-N05`–`HM-N10` delivery posture.
- **CC-N01 TUI command center and repair/doctor discovery completed**: The TUI `/` menu now shares one command catalog across native slash commands and project `commands/*.md` templates, so entries such as `/code-review` are auto-discovered, described, completed, and executed from the chat input. Added `command_discovery_v1` through `build_command_discovery_payload`, exposed it in `doctor_v1.command_center` and `api_doctor_summary_v1.command_center`, and expanded `doctor_sync_v1` / `repair_plan_v1` to cover the minimal command, skill, rule, and hook asset surface (`commands`, `skills`, `rules/common`, `rules/python`, `hooks/hooks.json`). Tests/smoke: `test_command_registry.py`, `test_tui_slash_suggester.py`, `test_doctor_cli.py`, `test_repair_cli.py`, repair/doctor CLI smoke.
- **CC-N02 structured `feedback bug` fields completed**: `cai-agent feedback bug` now accepts `--step`, `--expected`, `--actual`, and `--attachment`, persists sanitized `repro_steps`, `expected`, `actual`, and `attachments` in `feedback_bug_report_v1`, and reports structured capture counts in text mode. Tests/smoke: `test_feedback_cli.py`, feedback bug JSON/text CLI smoke.
- **CC-N02 feedback bundle and local triage flow started**: Added `cai-agent feedback bundle --dest ... --json`, exporting a redacted `feedback_bundle_v1` diagnostic bundle with recent feedback, `api_doctor_summary_v1`, `repair_plan_v1`, platform metadata, and redaction policy, plus a `feedback_bundle_export_v1` command result. `doctor --json` now includes `doctor_feedback_triage_v1`, tying together `doctor -> repair -> feedback bug -> feedback bundle` as the local support flow. Tests: `test_feedback_bundle_cli.py`, `test_feedback_cli.py`, `test_feedback_export.py`, `test_doctor_cli.py`.
- **Config discovery order fix**: `Settings.from_env()` now checks `CAI_WORKSPACE` and CLI `workspace_hint` after a direct cwd config and before walking cwd parents; `.tmp-*` / `pytest-*` transient directories also stop parent discovery at the temp boundary to avoid accidentally loading the repository root config. Tests: `test_model_profiles_config.py`.
- **CC-N01 repair / doctor install-surface work started**: Added `cai-agent repair --dry-run|--apply --json`, emitting `repair_plan_v1` / `repair_result_v1`, with conservative creation of missing `.cai/`, `.cai/gateway/`, `hooks/`, and missing `cai-agent.toml` from bundled templates. `doctor --json` now includes `doctor_install_v1` and `doctor_sync_v1`, and text doctor points operators to `repair --dry-run`. Tests: `test_repair_cli.py`, `test_doctor_cli.py`.
- **Next development queue docs sync**: Updated `DEVELOPER_TODOS.zh-CN.md`, `TEST_TODOS.zh-CN.md`, and `ISSUE_BACKLOG.zh-CN.md` to remove already completed `HM-N05`, `HM-N07`, `HM-N08`, `HM-N09`, `HM-N10`, `HM-N11`, and selected `ECC-N03/ECC-N04` work from the default start queue. The next tranche now focuses on `CC-N01`, `CC-N02`, `HM-N01`, `ECC-N01`, and `ECC-N02`.
- **Docs status and completed-feature sync pass**: Synchronized roadmap/todo/archive/implementation status docs for the latest completed tranche (`HM-N11-D01/D02`, `ECC-N04-D01/D02`) and refreshed the shared QA baseline to `803 passed, 3 subtests passed` plus green smoke.
- **ECC-N04-D02 ingest sanitizer policy draft**: Added `docs/ECC_04B_INGEST_SANITIZER_POLICY.zh-CN.md` (plus English companion) and `docs/schema/ecc_ingest_sanitizer_policy_v1.snapshot.json`, defining a metadata-first, default `deny-exec` sanitizer baseline with dangerous hook/script isolation before any trust-policy-based execution path is enabled.
- **ECC-N04-D01 asset-ingest registry draft snapshot**: Added `docs/schema/ecc_asset_registry_v1.snapshot.json` as a machine-readable `ecc_asset_registry_v1` draft covering source/license/signature/version/trust metadata, and updated `docs/schema/README.zh-CN.md` + `docs/schema/README.md` to mark it as a metadata-only `draft_snapshot` baseline for follow-up sanitizer/provenance work.
- **HM-N11-D02 runtime backend interface alignment**: Added `runtime_backend_interface_v1` in `runtime/registry.py` and exposed it through `runtime_registry_v1.interface` so `local/docker/ssh` share one explicit operation contract (`exec/exists/describe/ensure_workspace`) and aligned backend config-key surfaces for future cloud backend onboarding.
- **HM-N11-D01 cloud-runtime conditional gating document**: Expanded `docs/CLOUD_RUNTIME_OOS.zh-CN.md` with explicit go/no-go criteria (authorization, security, compliance, product value, engineering verification) and a clear "not in this cycle" boundary, plus synchronized the English companion `docs/CLOUD_RUNTIME_OOS.md`.
- **HM-N10-D05 Tool Gateway approval/policy/cost guards**: Added `cai-agent tools guard --json` (`tool_gateway_guard_v1`) and embedded guard metadata into `tool_provider_contract_v1.guard`; also added `--estimated-tokens` budget checks for `tools web-fetch` against `cost_budget_max_tokens` with explicit `cost_guard_exceeded` failures instead of silent execution.
- **HM-N10-D04 real web-provider end-to-end example**: Added `cai-agent tools web-fetch --url ... --json` as a real Tool Gateway provider execution path, reusing the existing `fetch_url` chain and enforcing registry-level web enable/disable state before dispatch (`tool_provider_web_fetch_v1`).
- **HM-N10-D03 MCP bridge reuse via existing presets**: Added `cai-agent tools bridge --preset ... --json` with `tool_mcp_bridge_v1`, reusing existing `mcp_presets` matching/report logic and the current `mcp_list_tools` path (instead of introducing a parallel bridge protocol).
- **HM-N10-D02 tool-category registry operations**: Added `tool_provider_registry_v1` plus persistent category toggles in `.cai/tool-providers.json`, and introduced `cai-agent tools list|enable|disable` for web/image/browser/tts categories with explicit `enabled_source` observability.
- **HM-N10-D01 unified tool-provider contract baseline**: Added `tool_provider_contract_v1` to unify web/image/browser/tts configuration and permission surfaces, exposed via `cai-agent tools contract --json`, and integrated into doctor payloads (`doctor_v1.tool_provider`, `api_doctor_summary_v1.tool_provider`) for a stable Tool Gateway machine-readable entrypoint.
- **HM-N09-D04 active memory-provider visibility convergence**: Added `memory_active_provider_v1` and wired it into `doctor`/API doctor summary, `profile_contract_v1`, and `export-v2` manifest fields (`active_memory_provider`, `active_memory_provider_source`) so active provider state is consistently observable across diagnostics, profiles, and exported artifacts.
- **HM-N09-D03 mock HTTP external provider validation path**: Upgraded `honcho_external` from placeholder-only to a mock-testable adapter via `memory provider test --id honcho_external`, probing `${CAI_MEMORY_EXTERNAL_MOCK_URL}/health` (optional `CAI_MEMORY_EXTERNAL_API_KEY`) and surfacing remote health/schema signals in `memory_provider_test_v1`.
- **HM-N09-D02 builtin local provider explicit registration**: Added `builtin_registry` (`memory_provider_builtin_registry_v1`) to memory provider contract/registry payloads to explicitly register builtin local providers and default selection. This removes implicit-only hardcoding and prepares a stable registration surface for upcoming external provider adapters.
- **HM-N09-D01 memory provider registry operations**: Added provider registry actions under `cai-agent memory provider list/use/test` while preserving backward-compatible `memory provider --json` contract output. Active-provider selection now persists in `.cai/memory-provider.json`, and provider test emits `memory_provider_test_v1` for CI/operator checks.
- **HM-N08-D04 voice boundary and cost guidance docs**: Updated `docs/rfc/HM_07A_VOICE_BOUNDARY.zh-CN.md` with explicit "available now vs OOS" voice scope, operator-facing cost/compliance notes, and rollout guidance tied to `voice config/check` plus Telegram `voice-reply` MVP.
- **HM-N08-D03 gateway voice-reply MVP (Telegram)**: Added `cai-agent gateway telegram voice-reply` as a minimal voice-reply loop that reuses `voice_provider_contract_v1` and sends voice messages through Telegram `sendVoice` using a configured `voice_file_id`. The command emits `gateway_telegram_voice_reply_v1` and returns exit `0/2` based on send success for CI/operator workflows.
- **HM-N08-D02 voice config/check CLI parity**: Added `cai-agent voice config` and `cai-agent voice check` to expose the same `voice_provider_contract_v1` surface used by doctor. `voice check` emits `voice_check_v1` and returns exit `0/2` based on configured health, enabling CI-friendly voice-provider preflight checks.
- **HM-N08-D01 voice provider contract baseline**: Added `voice_provider_contract_v1` (`cai_agent.voice`) with explicit STT/TTS/health sections and wired it into doctor outputs (`doctor_v1.voice`, `api_doctor_summary_v1.voice`). This creates a machine-readable voice-provider diagnosis surface without introducing runtime voice execution yet.
- **HM-N07-D04 unified federation summary output (CLI/API)**: Added `gateway_federation_summary_v1` as a shared aggregation surface via CLI (`cai-agent gateway federation-summary --json`) and API (`GET /v1/gateway/federation-summary`). The summary reuses federation + channel-monitoring data and exposes stable platforms/channels/error counters for operator automation.
- **HM-N07-D03 gateway proxy/routing minimal entrypoint**: Added `gateway_proxy_route_v1` dry-run route contract across both CLI (`cai-agent gateway route-preview`) and HTTP API (`POST /v1/gateway/route-preview`, dry_run-only). This introduces a stable source/route decision payload without expanding write-side routing yet.
- **HM-N07-D02 channel monitoring fields in production summary**: `gateway_production_summary_v1.platforms[*]` now includes `channel_monitoring` (`gateway_channel_monitoring_v1`) with per-channel `last_seen`, `latency_ms`, `error_count`, and `owner`, plus monitoring summary counters. This keeps gateway observability fields consistent for future federation routing and operator views.
- **HM-N07-D01 workspace federation schema baseline**: `gateway_maps_summarize_v1` now includes a nested `gateway_workspace_federation_v1` object (workspace-level platform binding/allowlist aggregates), and `gateway_production_summary_v1` now carries this `federation` payload for the same workspace snapshot. This establishes a stable multi-workspace summary contract for subsequent channel-monitoring and routing work.
- **ECC-N03-D02 home-sync manifest strengthening**: `export --target <cursor|codex|opencode>` now writes `cai-local-catalog.json` (`local_catalog_v1`) into export directories and records `local_catalog_schema_version` / `local_catalog_file` in `cai-export-manifest.json`. This binds export artifacts to a stable local catalog contract for follow-up sync/registry workflows.
- **ECC-N03-D01 local catalog schema baseline**: Added `local_catalog_v1` (`build_local_catalog_payload`) and wired `cai-agent ecc catalog` to export a unified local asset view for `rules` / `skills` / `hooks` / `plugins` with item counts, hooks parse status, and plugin-surface compatibility hints. This establishes a machine-readable base contract for subsequent home-sync/catalog work.
- **HM-N05-D05 new-platform prod-status convergence**: `gateway prod-status --json` (`gateway_production_summary_v1`) now includes Signal/Email/Matrix health/env/map summaries in the same production-state surface as Telegram/Discord/Slack/Teams. This closes the first multi-platform expansion slice by making the newly added adapters visible in one ops summary contract.
- **HM-N05-D04 Matrix adapter minimal room-path**: Added `cai_agent.gateway_matrix` and `cai-agent gateway matrix` subcommands (`bind/get/list/unbind`, `allow`, `send`, `receive`, `health`) with contracts `gateway_matrix_map_v1`, `gateway_matrix_health_v1`, and `gateway_matrix_messages_v1`. The MVP uses a local spool-backed room send/receive chain plus homeserver config-presence checks; `gateway platforms list --json` now marks Matrix as `mvp`.
- **HM-N05-D03 Email adapter minimal SMTP/IMAP path**: Added `cai_agent.gateway_email` and `cai-agent gateway email` subcommands (`bind/get/list/unbind`, `allow`, `send`, `receive`, `health`) with machine-readable contracts `gateway_email_map_v1`, `gateway_email_health_v1`, and `gateway_email_messages_v1`. The MVP uses a local spool-backed send/receive chain and config-presence checks for SMTP/IMAP fields; `gateway platforms list --json` now marks Email as `mvp` with adapter contract metadata.
- **HM-N05-D02 Signal adapter skeleton + CLI config surface**: Added `cai_agent.gateway_signal` with `gateway_signal_map_v1` / `gateway_signal_health_v1` and wired `cai-agent gateway signal` subcommands (`bind/get/list/unbind`, `allow`, `health`) for local map + config-presence checks. `gateway platforms list --json` now promotes Signal to `mvp` with env hints and shared adapter contract metadata.
- **HM-N05-D01 gateway adapter contract unification**: `gateway platforms list --json` now includes per-platform `adapter_contract` under `gateway_platform_adapter_contract_v1` (`send/receive/health/map/lifecycle` surfaces), plus top-level `adapter_contract_schema_version` for machine checks. `gateway prod-status --json` now carries the same adapter contract per platform row so ops consumers can rely on one capability contract across platforms.
- **HM-N04-D03 dashboard action audit hardening**: `ops_dashboard_action_audit_v1` now records actor/result metadata and supports audit-side filtering (`filter_action`, `filter_mode`, `ok`) through `mode=audit`, making action history easier to triage in local ops workflows.
- **HM-N04-D02 first writable dashboard actions**: Added `profile_switch_preview` (with `mode=apply`) to switch `[models].active` via workspace-local `cai-agent.toml`, and extended existing apply coverage for schedule reorder + gateway binding patch. This gives the dashboard action contract at least two real write paths (profile switch + map update) with shared audit logging.
- **HM-N04-D01 dashboard action contract (preview/apply/audit)**: Extended `GET /v1/ops/dashboard/interactions` with `mode=preview|apply|audit` under the same `ops_dashboard_interactions_v1` contract. Added minimal safe apply paths for `schedule_reorder_preview` and `gateway_bind_edit_preview`, plus local audit events (`ops_dashboard_action_audit_v1`) written to `.cai/ops-dashboard-actions.jsonl` and queryable through `mode=audit`.
- **HM-N03-D03 API/OPS auth convergence**: Added shared `resolve_bearer_token()` (`server_auth.py`) and applied it to both `api serve` and `ops serve` so auth config now converges across `CAI_API_TOKEN` and `CAI_OPS_API_TOKEN` (primary+fallback behavior in each server). Added `test_server_auth.py` and kept API/OPS suites green.
- **HM-N03-D02 API schema coverage update**: Added `api_profiles_v1.schema.json` for `GET /v1/profiles`, added schema assertions in `test_api_http_server.py`, and updated schema indexes in `docs/schema/README.md` and `docs/schema/README.zh-CN.md` so route docs and machine-readable contracts stay aligned.
- **HM-N03-D01 status-route expansion**: `api serve` now accepts `/health` as an alias of `/healthz` (same auth exemption) and adds `GET /v1/profiles` (`api_profiles_v1`) to expose active/subagent/planner profile IDs, profile list, and `profile_contract_v1` in one read-only endpoint for ops/tooling integration.
- **HM-N01-D03 active profile resolution unification**: Added shared profile-selection helpers (`get_profile_by_id`, `resolve_role_profile_id`) in `profiles.py` and switched `llm_factory.resolve_role_profile`, `models.fetch_models`, and TUI model-panel close flow to use the same resolution path, reducing per-entry drift across CLI/TUI/API/gateway-adjacent runtime code. Tests updated in `test_model_profiles_config.py`; related suites remain green.
- **HM-N01-D01 profile-home schema baseline**: `profile_contract_v1` now optionally exposes `profile_home_layout_v1` (`profile_homes` + `active_profile_home`) when a workspace root is available, giving doctor/API/CLI/TUI one canonical per-profile isolation path layout under `.cai/profiles/<id>/...`. Updated call sites: `doctor`, `api models summary`, `models list`, and TUI status/context surfaces. Tests: `test_model_profiles_config.py`, `test_model_profiles_cli.py`.
- **Docs backlog cleanup (Done items moved out of TODO)**: Removed completed MODEL-P0 / HM-N02 entries from **`docs/DEVELOPER_TODOS.zh-CN.md`** so it now tracks unfinished backlog only, and added migration notes in **`docs/COMPLETED_TASKS_ARCHIVE.zh-CN.md`**. Also refreshed recommended execution order to exclude completed work.
- **MODEL-P0 HTTP API schema (OpenAI + capabilities)**: Added **`api_models_capabilities_v1`**, **`api_openai_models_v1`**, **`api_openai_chat_completion_v1`**, and **`api_openai_chat_completion_chunk_v1`** JSON Schemas for `api serve` model routes; tests in **`test_api_http_server.py`**. Indexed in **`docs/schema/README.md`** / **`README.zh-CN.md`**.
- **MODEL-P0 doctor model-gateway schema**: Added **`doctor_model_gateway_v1.schema.json`** for the nested **`doctor_v1.model_gateway`** object (aligned with `doctor --json`); tests in **`test_doctor_cli.py`**. Indexed in **`docs/schema/README.md`** / **`README.zh-CN.md`**.
- **MODEL-P0 routing subcontracts as standalone schemas**: Added **`model_fallback_candidates_v1.schema.json`** and **`routing_explain_v1.schema.json`** under **`cai-agent/src/cai_agent/schemas/`**, matching the nested objects in **`models routing-test --json`** for single-file validation and documentation. **`test_model_routing.py`** asserts the `schema_version` consts and key fields. Indexed in **`docs/schema/README.md`** / **`README.zh-CN.md`**.
- **MODEL-P0 completion**: Closed the model integration foundation by adding **`models onboarding`** (**`model_onboarding_flow_v1`**) for the add → capabilities → ping → chat-smoke → use → routing-test command chain, **`doctor_model_gateway_v1`** for doctor-side capabilities / health-status / runbook guidance, and **`model_fallback_candidates_v1`** inside **`models routing-test --json`**. Fallbacks are explain-only (`auto_switch=false`) and include non-secret capability snapshots. New runbook: **`docs/MODEL_ONBOARDING_RUNBOOK.zh-CN.md`**. Tests: **`test_model_profiles_cli.py`**, **`test_model_routing.py`**, **`test_doctor_cli.py`**, smoke.
- **MODEL-P0 TUI closure**: The TUI model panel now renders the same non-secret capability view as CLI/API/doctor (`context`, streaming/tools/json, local/remote scope, cost hint) and refreshes inline `health=` after `t` ping. Tests: **`test_tui_model_panel.py`**.
- **MODEL-P0 provider registry hardening**: **`provider_registry_v1`** and provider readiness snapshots now include non-secret **`capabilities_hint`** payloads derived from the model gateway, so provider presets expose context / streaming / tools / local / cost hints without leaking `api_key` or `base_url`. Tests: **`test_provider_registry.py`**.
- **MODEL-P0 runbook discoverability**: The model onboarding runbook is now linked from both docs entrypoints and onboarding guides, with **`test_model_onboarding_docs.py`** guarding the runbook command chain and links.
- **MODEL-P0 onboarding validation**: **`models onboarding`** now rejects unknown presets early and includes a non-secret **`capabilities_hint`** in **`model_onboarding_flow_v1`**, matching the provider registry and model gateway capability view. Tests: **`test_model_profiles_cli.py`**.
- **MODEL-P0 schema hardening**: Added machine-readable schemas for **`model_onboarding_flow_v1`** and **`provider_registry_v1`** under **`cai-agent/src/cai_agent/schemas/`**, with tests guarding the schema consts and capability-hint fields.
- **MODEL-P0 capabilities schema hardening**: Added machine-readable schemas for **`model_capabilities_v1`** and **`model_capabilities_list_v1`**, with tests guarding the core capability fields shared by CLI/API/doctor/TUI/provider registry.
- **MODEL-P0 response schema hardening**: Added **`model_response_v1.schema.json`** for the normalized `ModelResponse` envelope (shared by gateway helpers and OpenAI-compatible chat completion internals), with tests; expanded English schema index [`docs/schema/README.md`](docs/schema/README.md) with MODEL-P0 schema file links.
- **OpenAI-compatible API server closure (MODEL-P0 / HM-N02)**: **`cai-agent api serve`** now exposes OpenAI-compatible **`GET /v1/models`** (**`api_openai_models_v1`**) and **`POST /v1/chat/completions`** backed by the unified **`ModelResponse`** / **`model_response_v1`** envelope. Chat supports both non-streaming **`api_openai_chat_completion_v1`** and minimal **`stream=true`** SSE chunks (**`api_openai_chat_completion_chunk_v1`** + `data: [DONE]`), with OpenAI-style `choices[]` / `usage` plus `cai_model_response` for provider/model/profile/latency details. **`CAI_METRICS_JSONL`** records **`api.chat_completions`** with provider/model/profile usage metadata. Tests: **`test_api_http_server.py`**, **`test_model_gateway.py`**, **`test_llm_factory_dispatch.py`**.
- **Bilingual documentation cleanup**: Added lightweight English companion summaries for current Chinese-primary docs, added Chinese migration and English schema companions, moved legacy Hermes/progress docs under **`docs/archive/legacy/`**, and updated active doc indexes to distinguish canonical docs, future queues, completed-task archives, and frozen historical records.
- **Todo archive cleanup**: Added **`docs/COMPLETED_TASKS_ARCHIVE.zh-CN.md`** / **`docs/COMPLETED_TASKS_ARCHIVE.md`** as the completed-task archive and trimmed **`docs/DEVELOPER_TODOS.zh-CN.md`** so it only carries unfinished work, future directions, OOS/conditional items, execution rules, and the current QA baseline.
- **Design backlog implementation (HM-04c / HM-03e / HM-05d)**: Added the preview-only **`ops_dashboard_interactions_v1`** HTTP contract for dashboard schedule reorder / gateway bind-edit dry runs, the multi-platform **`gateway prod-status --json`** **`gateway_production_summary_v1`** summary, and **`memory provider --json`** **`memory_provider_contract_v1`** for local entries / user-model provider coverage. Smoke now checks gateway prod-status and memory provider contracts. Tests: **`test_ops_http_server.py`**, **`test_gateway_lifecycle_cli.py`**, **`test_memory_provider_contract_cli.py`**.
- **Bilingual entrypoint cleanup (DOC-01c)**: Root README and docs README entrypoints now mention the newly delivered Teams gateway, docker/SSH runtime backend diagnostics, and plugin compatibility snapshot, with clearer English/Chinese pointers for implementation status, test TODOs, issue backlog, and the checked-in **`plugin_compat_matrix_v1.snapshot.json`**.
- **Plugin compat matrix CI snapshot (ECC-03c)**: Added **`scripts/gen_plugin_compat_snapshot.py`** with write and **`--check`** modes for **`docs/schema/plugin_compat_matrix_v1.snapshot.json`**. The snapshot embeds both **`plugin_compat_matrix_v1`** and **`plugin_compat_matrix_check_v1`**, making the ECC-03b governance gate usable as a deterministic CI dry-run. Smoke now runs the snapshot check. Tests: **`test_plugin_compat_matrix.py`**.
- **SSH runtime backend productization (HM-06c)**: **`runtime.ssh`** diagnostics now expose `ssh_binary_present`, key path configuration/existence, `known_hosts` path/existence, strict host key mode, connect timeout, and audit configuration through **`doctor_runtime_v1.describe`**. Optional **`runtime_ssh_audit_v1`** JSONL audit logging can be enabled with **`[runtime.ssh].audit_log_path`** / **`audit_label`**; command text is omitted by default unless **`audit_include_command=true`**. Tests: **`test_runtime_ssh_mock.py`**.
- **Docker runtime backend productization (HM-06b)**: **`runtime.docker`** now supports the existing `container` / `docker exec` path and a new `image` / `docker run --rm` path, with parsed **`[runtime.docker].workdir`**, **`volume_mounts`**, **`cpus`**, and **`memory`** settings. **`doctor --json`** / **`doctor_runtime_v1.describe`** now exposes mode/image/workdir/volume/limit details for local diagnostics. Smoke now checks **`runtime list --json`**. Tests: **`test_runtime_docker_mock.py`**, **`test_runtime_tool_dispatch.py`**.
- **Microsoft Teams Gateway (HM-03d)**: Added **`cai-agent gateway teams`** with `bind/get/list/unbind`, `allow`, `health`, `manifest`, and `serve-webhook`. The new **`cai_agent.gateway_teams`** module stores **`gateway_teams_map_v1`** in **`.cai/gateway/teams-session-map.json`**, exposes **`gateway_teams_health_v1`** (local map + app/tenant/webhook-secret config presence), generates a Teams app manifest scaffold (**`gateway_teams_manifest_v1`**), and handles lightweight Bot Framework Activity webhook messages (`help` / `ping` / `status` / `new`) without adding an SDK dependency. **`gateway platforms list`** and **`gateway maps`** now include Teams. Tests: **`test_gateway_discord_slack_cli.py`**, **`test_gateway_maps_summarize.py`**.
- **Read-only HTTP API extensions (HM-02c)**: **`cai-agent api serve`** now also exposes **`GET /v1/models/summary`** (**`api_models_summary_v1`** — `profile_contract_v1` whitelist + profile IDs; no `base_url` / `model` / `api_key`), **`GET /v1/plugins/surface`** (**`api_plugins_surface_v1`** — read-only surface; optional **`?compat=1`** appends **`plugin_compat_matrix_v1`**), and **`GET /v1/release/runbook`** (**`api_release_runbook_v1`** — release runbook whitelist without repo/workspace absolute paths). All new routes honor **`CAI_API_TOKEN`** Bearer auth just like the existing endpoints. Tests: **`test_api_http_server.py`**.
- **Model / status UX alignment (CC-03c)**: TUI **`#context-label`** (via **`build_context_label`** in `cai_agent.tui_session_strip`) now appends **`· route=sub`** / **`· route=pl`** / **`· route=sub+pl`** when subagent/planner overrides differ from active profile, plus **`· ⚠ migration`** when `profile_contract_v1.migration_state != ready`, truncated to 80 chars. TUI model-panel and `/use-model` success plus CLI **`models use`** now additionally print the shared **`profile_switched: <id>`** one-liner (via **`build_profile_switched_line`**). Tests: **`test_tui_session_strip.py`**, **`test_model_profiles_cli.py`**.
- **Plugin / version governance minimum gate (ECC-03b)**: **`build_plugin_compat_matrix()`** now exposes **`governance_rfc`** + **`maintenance_checklist`** (single source aligned with **`docs/rfc/ECC_03A_PLUGIN_VERSION_GOVERNANCE.zh-CN.md`** §2). New **`build_plugin_compat_matrix_check_v1()`** and CLI **`cai-agent plugins --compat-check`** return **`plugin_compat_matrix_check_v1`** (fields: `ok`, `missing_components`, `missing_targets`, `row_mismatches`, etc.) and exit `2` when the matrix drifts from `PLUGIN_COMPONENTS` × (cursor/codex/opencode). Schema: **`plugin_compat_matrix_v1.schema.json`** adds optional `governance_rfc` / `maintenance_checklist`. Tests: **`test_plugin_compat_matrix.py`**.
- **Undeveloped-feature queue (2026-04-25)**: The batch initially organized remaining work across P0/P1/OOS items and mirrored it in **`ROADMAP_EXECUTION`** §10 and **`GAP_TRACKER.md`**. Completed items are now archived in **`docs/COMPLETED_TASKS_ARCHIVE.zh-CN.md`**, while **`DEVELOPER_TODOS`** only keeps future work and OOS/conditional directions.
- **Explore batch closed (HM-03c / ECC-03a / HM-06a / HM-07a)**: Added assessment/design RFCs under **`docs/rfc/`**: **`HM_03C_NEXT_GATEWAY_PLATFORMS.zh-CN.md`**, **`ECC_03A_PLUGIN_VERSION_GOVERNANCE.zh-CN.md`**, **`HM_06A_RUNTIME_BACKEND_ASSESSMENT.zh-CN.md`**, **`HM_07A_VOICE_BOUNDARY.zh-CN.md`**; roadmap §10 issues marked **Done** (document deliverables) and archived in **`docs/COMPLETED_TASKS_ARCHIVE.zh-CN.md`**.
- **Minimal read-only HTTP API (HM-02b)**: New **`cai-agent api serve`** backed by **`cai_agent.api_http_server`** — **`GET /healthz`**, **`GET /v1/status`** (**`api_status_v1`** + **`gateway_summary_v1`**), **`GET /v1/doctor/summary`** (**`api_doctor_summary_v1`**, no raw **`base_url`/`model`**), **`POST /v1/tasks/run-due`** (**`api_tasks_run_due_v1`**, **`dry_run` only**; execute returns **403**). Env: **`CAI_API_PORT`** (default **8788**), optional **`CAI_API_TOKEN`** (**Bearer**; **`/healthz`** exempt). Tests: **`test_api_http_server.py`**; smoke runs **`api serve --help`**.
- **Recall evaluation polish (HM-05b)**: `recall --evaluate` no longer requires `--query`; JSON **`recall_evaluation_v1`** unchanged. Smoke covers **`recall --evaluate --json`**.
- **Memory policy visibility (HM-05c)**: Text **`doctor`** prints **`[memory.policy]`** lines; **`release-ga --with-memory-policy`** covered by tests (**`memory_policy_entries`** gate).
- **ECC install/export/share narrative (ECC-01b)**: **`CROSS_HARNESS_COMPATIBILITY*.md`** adds a numbered install → ECC layout → export → share flow.
- **Cost report + compact policy explain (ECC-02b)**: **`cost report --json`** now embeds **`compact_policy_explain_v1`** (aligned with **`graph`** compact / ~85% budget hint thresholds). **`cost report`** without **`--json`** prints a text summary instead of erroring.
- **Design RFCs**: **`docs/rfc/HM_02_MINIMAL_SERVER_CONTRACT.zh-CN.md`** (**HM-02a**), **`docs/rfc/CC_03B_MODEL_STATUS_UX.zh-CN.md`** (**CC-03b**).

### 0.7.0 (2026-04-23)

- **TUI task/status/session UX (CC-03a)**: New **`cai_agent.tui_session_strip`** shared copy for **`/help`**, welcome, **`/sessions`**, post-**`/load`** hints, **`/retry`**, input placeholder, **`/tasks`** task-board header, and **`#context-label`** (`<profile> · 上下文`) so task board, context bar, and “continue” guidance stay aligned.
- **WebSearch / Notebook TUI entry (CC-01b)**: New TUI slash **`/mcp-presets`** plus **`format_tui_mcp_web_notebook_quickstart()`** surfaced from **`/help`**, **`/status`**, and the **task board** footer; **`mcp-check --help`** epilog points at preset + doc paths.
- **Structured `/bug`-style feedback (CC-02b)**: New **`cai-agent feedback bug`** with **`--detail`** / **`--detail-file`**, **`--category`** (`crash` / `wrong_result` / `ux` / `docs` / `perf` / `security` / `other`), optional **`--attach-doctor-hint`**, and JSON **`feedback_bug_report_v1`** rows in **`.cai/feedback.jsonl`**. **`sanitize_feedback_text`** redacts common tokens/secrets before write; **`release_runbook_v1.runbook_steps`** documents the optional capture step.
- **Release feedback summary (REL-01b)**: `doctor --json` now reuses **`release_runbook.feedback`** (single `feedback_stats` pass via **`build_release_runbook_payload`**) so **`feedback`** and **`release_runbook.feedback`** stay identical. New **`cai-agent feedback stats`** (**`--json`** → same **`feedback_stats_v1`** as doctor) for a dedicated CLI entrypoint.
- **Routing / budget explain (ECC-02a)**: `models routing-test` no longer requires `--json` (prints `effective_profile_id` + Chinese summary); JSON adds nested **`explain`** (**`routing_explain_v1`**). `cost budget` JSON adds **`explain`** (**`cost_budget_explain_v1`**) + **`active_profile_id`**. Helpers: **`build_routing_explain_v1`**, **`build_cost_budget_explain_v1`**. Schema: **`models_routing_test_v1`** documents optional **`explain`**. Tests/smoke/docs updated.
- **ECC asset layout (ECC-01a)**: New `cai_agent.ecc_layout` (shared paths for rules/skills/hooks resolution); CLI **`cai-agent ecc layout`** → **`ecc_asset_layout_v1`**, **`ecc scaffold`** → **`ecc_scaffold_result_v1`** with packaged **`templates/ecc/*`**; `hook_runtime.resolve_hooks_json_path` now uses **`iter_hooks_json_paths`**; docs **`CROSS_HARNESS_COMPATIBILITY*.md`** + schema README / metrics; smoke runs **`ecc … layout --json`**. Tests: **`test_ecc_layout_cli.py`**.
- **User model store loop (HM-05a)**: `memory user-model store init|list` → `memory_user_model_store_init_v1` / `memory_user_model_store_list_v1`; `learn`/`query` JSON now includes `store_path` (and `needle` for query); empty `learn` belief → exit **2**; `memory user-model export --with-store` attaches `user_model_store_snapshot_v1` under **`user_model_store`** in **`user_model_bundle_v1`**. Tests: **`test_memory_user_model_store_cli.py`**; smoke extended.
- **Discord gateway (HM-03a)**: `cai-agent gateway discord health` → `gateway_discord_health_v1` (local map + optional `GET /users/@me`); wired **`register-commands`** / **`list-commands`** CLI to `gateway_discord` (previously doc-only); **`bind`** accepts **`--guild-id`** / **`--label`**; **`doctor`** `cai_dir_health.discord_map_summary`; troubleshooting runbook in **`docs/GATEWAY_DISCORD_TELEGRAM_PARITY.zh-CN.md`**; smoke covers **`discord list`** + **`discord health`**.
- **Install / upgrade guidance**: `init --json` now returns `support_docs` and `next_steps`, while `doctor --json` includes `installation_guidance` so first-run and upgrade users can discover onboarding, docs index, and changelog pointers from the CLI itself.
- **MCP preset onboarding**: `mcp-check --preset` now supports the combined `websearch/notebook` path, emits `presets[]` details plus onboarding/doc pointers, and prints richer preset templates and next-step hints for WebSearch / Notebook onboarding.
- **Release runbook summary**: `doctor --json` and `release-ga --json` now include a shared `release_runbook_v1` block with the recommended command order, changelog bilingual/semantic status, write-back targets, and feedback summary. `release-ga` also promotes changelog sync into GA checks and adds `failed_check_details` for human-readable failures.
- **Release changelog report**: `release-changelog --json --semantic` now emits a dedicated `release_changelog_report_v1` envelope with nested bilingual/semantic results plus a trimmed runbook summary, and the text-mode command prints the next release steps directly in the terminal.
- **Profile contract summary**: `doctor --json` and `models list --json` now expose a shared `profile_contract_v1` block so HM-01 work has one source of truth for explicit vs legacy profiles, active-profile selection order, fallback behavior, and migration status.
- **Ops / gateway shared summary**: `board --json`, `ops dashboard --json`, and `gateway status --json` now share `gateway_summary_v1`, aligning read-side fields such as `status`, `bindings_count`, `webhook_running`, and allowlist state for HM-04.
- **Session events envelope (`run_schema_version=1.1`)**: `run`/`continue`/`command`/`agent`/`fix-build` JSON output now wraps `events` in a stable `run_events_envelope_v1` object (`schema_version` + `items[]`) instead of a bare list. `observe` / `sessions` consume both formats via `normalize_session_run_events`. All failure paths (`goal_empty`, `load_session_failed`, `interrupted`, etc.) also emit the structured envelope.
- **TUI task board** (`/tasks` + `Ctrl+B`): new read-only panel showing `.cai-schedule.json` tasks and `.cai/last-workflow.json` workflow snapshot — `tui_task_board.py`.
- **Hooks `script` auto-execution**: `hook_runtime.py` now resolves a `script` field (`.py` / `.sh` / `.ps1` / `.cmd` / `.bat`) in addition to `command[]`, with path-escape guard and platform normalization. `hooks/README.md` updated.
- **`memory validate-entries`**: new subcommand → `memory_entries_file_validate_v1` JSON; exit 0 if clean, exit 2 if invalid lines; backed by `build_memory_entries_jsonl_validate_report`.
- **`memory/entries.jsonl` write guard**: before appending, `append_memory_entry` / `import_memory_entries_bundle` require the existing file to pass the same line-level checks as `validate-entries` (`require_memory_entries_jsonl_clean_before_write`); `memory extract` preflight fails with JSON `ok: false` + exit **2** when dirty. Escape hatch: **`CAI_MEMORY_ALLOW_DIRTY_ENTRIES_JSONL=1`**.
- **`auto_extract_skill_after_task` LLM draft**: optional **`settings`** enables LLM-generated skill markdown via `chat_completion_by_role`; JSON result includes **`draft_method`** (`llm` | `template`).
- **`insights --json --cross-domain`**: `insights_cross_domain_v1` now exposes **`recall_hit_rate_metric_kind`** (`index_probe`), **`recall_hit_rate_metric_note`**, and per-day **`metric_kind`** / **`metric_unavailability_reason`** on **`recall_hit_rate_trend`** rows so index substring probe rates are not mistaken for **`recall`** query hit-rate.
- **Docs**: added **`docs/qa/T7_RELEASE_GATE_CHECKLIST.zh-CN.md`** (manual release gate aligned with PRODUCT_PLAN T7).
- **`fetch_url` max redirects**: **`[fetch_url].max_redirects`** (int **1–50**, default **20**) or **`CAI_FETCH_URL_MAX_REDIRECTS`** env override (clamped); wired to **`httpx.Client(max_redirects=…)`**. **`doctor --json`** includes **`fetch_url_max_redirects`**; text doctor prints it next to fetch_url settings.
- **`fetch_url` post-DNS SSRF guard**: before **`httpx`**, **`socket.getaddrinfo`** is checked; if **any** resolved address is private / loopback / link-local / reserved / multicast / unspecified, the request is rejected (mitigates DNS rebinding). Corporate split-horizon: **`[fetch_url].allow_private_resolved_ips=true`** or **`CAI_FETCH_URL_ALLOW_PRIVATE_RESOLVED_IPS=1`** skips this check. **`doctor`** JSON/text exposes **`fetch_url_allow_private_resolved_ips`**.
- **Tools registry doc generation**: **`cai_agent/tools_registry_doc.py`** holds **`BUILTIN_TOOLS_DOC_ROWS`**; **`scripts/gen_tools_registry_zh.py`** regenerates **`docs/TOOLS_REGISTRY.zh-CN.md`**; **`tools.DISPATCH_TOOL_NAMES`** frozen set must match dispatch; CI runs **`--check`**. Tests: **`test_tools_registry_doc_sync.py`**.
- **Docs**: **`docs/OPS_DYNAMIC_WEB_API.zh-CN.md`** — read-only HTTP contract (**`GET /v1/ops/dashboard`** / **`dashboard.html`**) aligned with **`ops_dashboard_v1`**, optional **`CAI_OPS_API_TOKEN`** bearer auth, Phase A–C scope (Phase A/B implemented in-repo; Phase C still future).
- **`ops serve` (read-only HTTP sidecar)**: **`cai-agent ops serve`** exposes **`/v1/ops/dashboard`** (JSON) and **`/v1/ops/dashboard.html`** via stdlib **`ThreadingHTTPServer`**; **`--allow-workspace`** allowlist (defaults to cwd); **`CAI_OPS_API_TOKEN`** when set. Implementation: **`cai_agent.ops_http_server`**. Tests: **`test_ops_http_server.py`**.
- **`ops dashboard --format html`**: optional **`--html-refresh-seconds`** embeds **`meta http-equiv=refresh`** (Phase A). **`ops serve`** HTML route accepts **`html_refresh_seconds`** query param.
- **`memory user-model export`**: stdout **`user_model_bundle_v1`** (nested **`memory_user_model_v1`** **`overview`**, **`bundle_kind=behavior_overview`**). Tests: **`test_memory_user_model_export.py`**. **`smoke_new_features`** covers export.
- **TUI `/tasks` panel**: **`render_task_board_markup`** now uses **`build_board_payload`** + **`attach_failed_summary`** + **`attach_status_summary`** (same sources as **`board`** defaults), **`enrich_schedule_tasks_for_display`** for **`.cai-schedule.json`** (same as **`schedule list`**), richer workflow step list; tests **`test_tui_task_board_render.py`**.
- **Docs**: **`docs/CLOUD_RUNTIME_OOS.zh-CN.md`** (Modal/Daytona cloud sandboxes **OOS** by default; alternatives); **`docs/MODEL_ROUTING_RULES.zh-CN.md`** / **`docs/MODEL_ROUTING_RULES.md`** (**`[models.routing]`** + **`models routing-test`**). **PARITY_MATRIX** L3 cloud row = **`OOS`**; L2 cost row links routing docs.
- **Docs (wave α)**: **`docs/MEMORY_TTL_CONFIDENCE_POLICY.zh-CN.md`** (TTL / confidence vs `prune`/`state`/`health`); **`docs/CHANGELOG_SYNC.zh-CN.md`** + **`.github/pull_request_template.md`** (bilingual changelog + PR checklist); **`docs/qa/GATEWAY_500_MSG_STRESS_RUNBOOK.zh-CN.md`** + **`docs/qa/runs/TEMPLATE_GATEWAY_S8_AC3.zh-CN.md`** (S8-02 AC3); T7 / sprint8 / PRODUCT_PLAN / PRODUCT_GAP / runs README cross-links.
- **`memory extract --structured`**: optional LLM-structured extraction; degrades to heuristic rules in mock/no-key mode via `extract_memory_entries_structured`.
- **Subagent IO schema v1.1**: `workflow` output bumps `subagent_io_schema_version` to `1.1`; each step now includes `agent_template_id` (matched against `agents/` catalog) and optional `rpc_step_input`/`rpc_step_output` from `protocol`.
- **Workflow post quality-gate linkage**: workflow JSON root now accepts `quality_gate` (`true` or an object overriding `compile` / `test` / `lint` / `typecheck` / `security_scan` / `report_dir`). When the workflow itself succeeds, `run_workflow` executes a post `quality-gate`, appends `quality_gate` summary + optional `post_gate` (`quality_gate_result_v1`), emits `workflow.quality_gate.*` events, and marks the workflow failed on gate failure (`workflow_quality_gate_failed`).
- **`plan --json` stable schema**: `goal_empty` error now includes `"task": null`; all error branches include `plan_schema_version`.
- **`hooks.json` auto-execution (non-dry-run)**: `hooks run-event` without `--dry-run` now actually executes matching scripts/commands (was always possible but now also wired to `script` field).
- **Quality-gate frontend monorepo**: `CAI_QG_FRONTEND_MONOREPO=1` + `package.json` present → auto-appends `npm run -ws --if-present lint` to quality-gate checks.
- **`export --ecc-diff`**: `export_ecc_dir_diff_v1` JSON comparing repo source dirs vs `.cursor/cai-agent-export`; no file writes.
- **`skills hub install`**: new subcommand to copy selected entries from a `skills_hub_manifest_v1` JSON into a target directory (`--only`, `--dry-run`).
- **Progress ring buffer** (`progress_ring.py`): all `graph.py` `_emit` calls now also push to a global `ProgressRing`; `run --json` output includes `progress_ring` field with `phase_distribution` summary; ring is reset per invocation.
- **Compact + cost budget linkage**: when `compact_hint` fires, if accumulated tokens exceed 85% of `cost_budget_max_tokens`, an additional cost hint message is injected into the conversation.
- **`models suggest`**: heuristic task-description–to–profile recommendation (`models_suggest_v1` JSON), matching `安全/review/plan/fast/analysis` keyword groups.
- **`[models.routing]` (declarative)**: TOML `[[models.routing.rules]]` with `roles` / `goal_regex` / `goal_substring` / **`cost_budget_remaining_tokens_below`** / `profile` → **`Settings.model_routing_rules`**; **`chat_completion_by_role`** applies the first AND-match (goal from first `user` message when present; cost uses **`[cost].budget_max_tokens`** vs **`get_usage_counters()["total_tokens"]`**). **`cai-agent models routing-test`** supports optional **`--goal`**, **`--total-tokens-used`** (drives **`routing_total_tokens_used_override`**), **`--json`** → **`models_routing_test_v1`**. **`doctor --json`**: **`model_routing_enabled`**, **`model_routing_rules_count`**. Implementation: **`cai_agent.model_routing`**, **`resolve_effective_profile_for_llm`**. Docs: **`docs/MODEL_ROUTING_RULES.md`** / **`docs/MODEL_ROUTING_RULES.zh-CN.md`**; schema index row for **`routing-test`**; JSON Schema **`cai-agent/src/cai_agent/schemas/models_routing_test_v1.schema.json`**.
- **`security-scan --badge`**: appends `security_badge_v1` (shields.io-compatible) JSON line; `message` and `color` reflect finding count and severity.
- **`memory user-model` upgraded** from stub to behavior extraction: parses tool frequencies, error rates, and recent goal previews from sessions → `honcho_parity: behavior_extract`.
- **`doctor` `.cai/` health**: `build_doctor_cai_dir_health` adds gateway map existence + hooks.json validity checks to both JSON and text doctor output.
- **Plugin compat matrix (`plugin_compat_matrix_v1`)**: `cai-agent plugins --json --with-compat-matrix` appends a machine-readable Cursor/Codex/OpenCode vs extension-kind matrix; `doctor --json` includes a **`plugins`** bundle (`doctor_plugins_bundle_v1`) with the same **`compat_matrix`**. JSON Schema: `cai-agent/src/cai_agent/schemas/plugin_compat_matrix_v1.schema.json`. Human-oriented notes: `docs/PLUGIN_COMPAT_MATRIX.zh-CN.md` and **`docs/PLUGIN_COMPAT_MATRIX.md`** (English); cross-harness narrative: **`docs/CROSS_HARNESS_COMPATIBILITY.zh-CN.md`** / **`docs/CROSS_HARNESS_COMPATIBILITY.md`**. Matrix JSON includes optional **`doc_anchor_en`** / **`detail_doc_en`** alongside the Chinese paths.
- **Skills auto-suggest hook**: set `CAI_SKILLS_AUTO_SUGGEST=1` to auto-run `build_skill_evolution_suggest` on `session_end` and drop a draft under `skills/_evolution_*.md`.
- **`ONBOARDING.zh-CN.md`**: new step-by-step onboarding path doc (vision → fetch_url/MCP → doctor → run).
- **Tools registry doc**: `docs/TOOLS_REGISTRY.zh-CN.md` catalogues all 13 tools with permission keys.
- **Tests**: `test_memory_validate_entries_cli.py`, `test_hook_runtime.py` (script hook), updates to `test_plan_sessions_cli.py` / `test_cli_workflow.py` / `test_gateway_telegram_execute_goal.py` / `test_session_observe.py` / `test_stats_json.py` / `test_cli_board_and_workflow_snapshot.py` for schema version bumps and workflow post-gate coverage; `test_memory_entry_validate.py` / `test_memory_state_machine_cli.py` / `test_memory_import_entries_cli.py` (entries write guard); `test_skills_auto_extract_hub_serve.py` (LLM draft path); `test_tools_fetch_url.py` (**`max_redirects`** / env clamp / post-DNS SSRF + `allow_private_resolved_ips`); `test_tools_registry_doc_sync.py` (registry vs `DISPATCH_TOOL_NAMES`); `test_tui_task_board_render.py` (TUI `/tasks` markup builder); `test_plugin_compat_matrix.py`; `test_doctor_cli.py` (**`doctor` `plugins.compat_matrix`**); `test_model_routing.py` + **`test_llm_factory_dispatch.py`** (**`[models.routing]`** overlay); **`test_ops_http_server.py`**; **`test_ops_dashboard_html.py`** (HTML refresh CLI); **`test_memory_user_model_export.py`**.

### 0.6.18 (2026-04-23)

- **Multi-platform gateway / skills evolution / memory (MVP slice)**: **`gateway platforms list --json`** now includes **`telegram_webhook_pid_exists`**, **`telegram_bot_token_env_present`**, and per-stub-platform **`env_present`** (boolean only, no secret values). New **`skills hub suggest`** → **`skills_evolution_suggest_v1`** (optional **`--write`** to create a non-destructive draft under **`skills/_evolution_*.md`**). New **`memory user-model --json`** → **`memory_user_model_v1`** (session mtime window + optional **`.cai/user-model.json`** overlay; **`honcho_parity: stub`**). **`CAI_METRICS_JSONL`**: **`skills.evolution_suggest`**, **`memory.user_model`**. **`smoke_new_features`** extended.
- **Tests**: **`test_gateway_user_model_skills_evolution.py`**, **`test_metrics_jsonl`** additions.

### 0.6.17 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: **`CAI_METRICS_JSONL`** now also records **`init.apply`**, **`models.*`** (per subcommand, e.g. **`models.list`** / **`models.fetch`**), **`workflow.run`** (including **`run_workflow` exceptions**), **`release_ga.gate`**, and **`ui.tui`**. **`docs/schema/METRICS_JSON.zh-CN.md`** and schema index updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the new paths.

### 0.6.16 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: **`CAI_METRICS_JSONL`** now also records **`sessions.list`**, **`stats.summary`**, **`insights.summary` / `insights.cross_domain`**, **`plugins.surface`**, **`skills.hub_manifest`**, **`commands.list`**, **`agents.list`**, **`doctor.run`**, **`plan.generate`**, **`cost.budget`**, **`export.target`**, **`observe.report`** (standalone **`observe-report`**), **`ops.dashboard`**, **`board.summary`**, and **`hooks.list`**. **`docs/schema/METRICS_JSON.zh-CN.md`** updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the new paths.

### 0.6.15 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: **`CAI_METRICS_JSONL`** now also records **`mcp.check`**, **`hooks.run_event`**, and **`gateway.telegram.serve_webhook`** (after a normal **`serve-webhook`** server run). **`docs/schema/METRICS_JSON.zh-CN.md`** updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the three paths.

### 0.6.14 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: **`CAI_METRICS_JSONL`** now also records **`memory.extract` / `list` / `instincts` / `search` / `prune` / `export` / `import` / `export_entries` / `import_entries`**, **`quality_gate.run`**, **`security_scan.run`**, **`gateway.telegram.resolve_update`**, and **`schedule.add_memory_nudge`**. **`docs/schema/METRICS_JSON.zh-CN.md`** updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the new paths.

### 0.6.13 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: **`CAI_METRICS_JSONL`** now also records **`command.invoke` / `agent.invoke` / `fix-build.invoke`** (same shape as **`run.invoke`**), **`memory.state` / `memory.nudge` / `memory.nudge_report`**, **`recall_index.benchmark` / `info` / `clear` / `doctor`**, **`schedule.rm` / `schedule.run_due` / `schedule.daemon`**, and **`gateway.telegram`** paths **`bind` / `get` / `unbind` / `continue_hint` / `allow_add` / `allow_list` / `allow_rm`**. **`docs/schema/METRICS_JSON.zh-CN.md`** updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the new paths.

### 0.6.12 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: When **`CAI_METRICS_JSONL`** is set, **`_maybe_metrics_cli`** also appends **`recall_index.build` / `recall_index.refresh` / `recall_index.search`**, **`schedule.list`**, **`schedule.add`**, **`gateway.telegram.list`**, and **`run.invoke` / `continue.invoke`** (latency, coarse **`tokens`**, **`success`** for run/continue). **`docs/schema/METRICS_JSON.zh-CN.md`** and schema index updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the new paths.

### 0.6.11 (2026-04-23)

- **Observability (Hermes S7-01 AC2 extension)**: When **`CAI_METRICS_JSONL`** is set, **`_maybe_metrics_cli`** appends **`memory.health`**, **`recall.query`**, **`schedule.stats`**, and **`gateway.status`** events (latency + coarse **`tokens`** hint). **`docs/schema/METRICS_JSON.zh-CN.md`** and schema index updated.
- **Tests**: **`test_metrics_jsonl.py`** covers the four new paths.

### 0.6.10 (2026-04-23)

- **GA perf gate (Hermes S8-02)**: **[`scripts/perf_ga_gate.py`](scripts/perf_ga_gate.py)** runs the recall **200**-session benchmark thresholds (same logic as **`perf_recall_bench.py`**); optional **`--pytest-daemon`** runs the daemon stability test. **`tests/test_perf_ga_s8_02.py`**: **PERF-GA-001/002** (recall scan + index search) and **PERF-GA-003** (`schedule daemon` **100** cycles with mocked execute + sleep). **AC3** (gateway **500** messages) remains manual / harness-specific per sprint plan.
- **GA security gate (Hermes S8-03)**: **[`scripts/security_ga_gate.py`](scripts/security_ga_gate.py)** runs **`run_security_scan`** over **`cai-agent/src`** plus a quoted **`sk-…`** literal probe. **`tests/test_sec_ga_s8_03.py`**. Allowlist deny path remains **`tests/test_gateway_telegram_cli.py`** (**`not_allowed`** / **SEC-GA-004**).

### 0.6.9 (2026-04-23)

- **Docs (Hermes S8-04)**: Added **[`docs/MIGRATION_GUIDE.md`](docs/MIGRATION_GUIDE.md)** (0.5.x → 0.6.x: JSON envelopes, exit codes, schedule audit, recall). **`README.md` / `README.zh-CN.md`**: version requirements + link to the migration guide. **`CHANGELOG.md` / `CHANGELOG.zh-CN.md`** §**0.6.0**: explicit **Breaking changes**, **New CLI (0.6.x train)**, and **Deprecations** subsections for GA doc gates.
- **Tests**: `tests/test_migration_guide_present.py` guards the migration doc in the monorepo layout.

### 0.6.8 (2026-04-23)

- **Observability (Hermes S7-04)**: **`cai-agent observe export`** (`--days`, **`--format` csv|json|markdown**, **`-o`**) → **`observe_export_v1`** with per-day **`rows`** (sessions, tokens, schedule ok/fail, **memory** score/grade). **`cai_agent.observe_export`**, **`schedule.aggregate_schedule_audit_by_calendar_day_utc`** reuse. **`smoke_new_features`** writes **`observe-export.json`** in the sessions/observe temp dir.
- **Tests**: **`test_observe_export_cli.py`**.

### 0.6.7 (2026-04-23)

- **Observability (Hermes S7-03)**: **`cai-agent insights --json --cross-domain`** → **`insights_cross_domain_v1`** with **`recall_hit_rate_trend`** (`.cai-recall-index.json` substring probe **`the`** / `index_missing`), **`memory_health_trend`** (per-day **`build_memory_health_payload`** via session **mtime** window), **`schedule_success_trend`** (**`aggregate_schedule_audit_by_calendar_day_utc`**). **`build_memory_health_payload`** gains optional **`reference_now` / `session_mtime_start` / `session_mtime_end_exclusive`**. **`smoke_new_features`** covers cross-domain.
- **Tests**: **`test_insights_cross_domain.py`**.

### 0.6.6 (2026-04-23)

- **Observability (Hermes S7-01 / S7-02)**: **`cai_agent.metrics`** (`metrics_schema_v1`) with optional append to **`CAI_METRICS_JSONL`** after **`observe`** / **`observe report`**. **`cai-agent observe report`** (`--days`, **`--format` json|markdown**, **`-o`**) builds **`observe_ops_report_v1`** (root **`schema_version` `1.0`**). **`build_observe_payload`** gains time-window filtering plus **`tool_errors_total` / `tool_errors_top`** in **`aggregates`**.
- **Docs / smoke / tests**: **`docs/schema/METRICS_JSON.zh-CN.md`**; schema index + **`smoke_new_features.py`** **`observe report`**; **`test_observe_ops_report_cli.py`**, **`test_metrics_jsonl.py`**.

### 0.6.5 (2026-04-23)

- **Gateway (Hermes S6-04)**: **`cai-agent gateway telegram continue-hint`** prints JSON (**`gateway_telegram_continue_hint_v1`**) with shell-safe **`continue_cli`** lines for bound **`session_file`** paths (optional **`--chat-id`** + **`--user-id`**, or list all). **`/help`** / **`/new`** slash text points to this command. QA plan **`docs/qa/sprint6-gateway-telegram-testplan.md`** adds **GTW-CONT-001~003**.
- **Tests / smoke**: **`test_gateway_telegram_cli.py`** continue-hint cases; **`smoke_new_features.py`** asserts **`continue-hint --json`**.

### 0.6.4 (2026-04-23)

- **Gateway (Hermes S6-02)**: **`serve-webhook --execute-on-update`** now runs **`_execute_gateway_telegram_goal`**: same load/append/save path as CLI **`run`/`continue`** on the bound **`session_file`** (new file created on first message). **`reply_template`** receives the full **`{answer}`** (Telegram send still chunked). Slash **`/stop`**: default text points to **`gateway stop`**; optional **`CAI_TELEGRAM_STOP_WEBHOOK=1`** plus **`CAI_TELEGRAM_ADMIN_USER_IDS`** (comma-separated Telegram **`user_id`**) allows in-chat **`gateway_lifecycle.stop_webhook_subprocess`**. **`/help`** documents **`/stop`** and run-like execution.
- **Tests**: **`tests/test_gateway_telegram_execute_goal.py`** (session persistence, **`/stop`** paths).

### 0.6.3 (2026-04-23)

- **Gateway lifecycle (Hermes S6-01)**: **`cai-agent gateway setup|start|status|stop`** backed by **`cai_agent.gateway_lifecycle`** (writes **`gateway_telegram_config_v1`** to **`.cai/gateway/telegram-config.json`**, PID **`.cai/gateway/telegram-webhook.pid`**). **`setup`** mirrors **`serve-webhook`** flags and optional **`--allow-chat-id`**. **`start`** spawns **`gateway telegram serve-webhook`** detached (logs under **`.cai/gateway/`**). **`-w`/`--workspace`** on **`telegram`**, **`platforms`**, and lifecycle subcommands sets the workspace root.
- **Gateway (Hermes S6-02 partial)**: Webhook deny / execution replies use **`_telegram_send_text_chunked`** (~3900 chars). Slash-first messages (**`/ping`**, **`/status`**, **`/help`**, **`/start`**, **`/new`**) get inline replies without running **`_execute_scheduled_goal`**.
- **Tests / smoke**: **`tests/test_gateway_lifecycle_cli.py`**; **`scripts/smoke_new_features.py`** asserts **`gateway status --json`** (**`gateway_lifecycle_status_v1`**).

### 0.6.2 (2026-04-23)

- **Gateway (Hermes S6-03)**: Optional root **`allowed_chat_ids`** on **`gateway_telegram_map_v1`** map file. **`gateway telegram allow add|list|rm`**. **`resolve-update`** / **`serve-webhook`** return **`not_allowed`** when the list is non-empty and **`chat_id`** is missing. **`list --json`** adds **`allowed_chat_ids`** and **`allowlist_enabled`**. **`serve-webhook`**: wired missing kwargs (**`--reply-on-execution`**, **`--telegram-bot-token`** / **`CAI_TELEGRAM_BOT_TOKEN`**, **`--reply-template`**), plus **`--reply-on-deny`** / **`--deny-message`** for optional Telegram rejection text.
- **Tests / smoke**: `test_gateway_telegram_cli.py` allowlist cases; smoke asserts **`list`** allowlist fields.

### 0.6.1 (2026-04-23)

- **Gateway (dev item 24 MVP)**: `cai-agent gateway platforms list --json` → **`gateway_platforms_v1`** (Telegram `full`, Discord/Slack `stub`, others `planned`).
- **Skills Hub (dev item 25 MVP)**: `cai-agent skills hub manifest --json` → **`skills_hub_manifest_v1`** (exportable listing from workspace `skills/`). Automatic skill evolution remains out of scope.
- **Ops dashboard (dev item 26 MVP)**: `cai-agent ops dashboard --json` → **`ops_dashboard_v1`** (nested **`board_v1`**, **`schedule_stats_v1`**, **`cost_aggregate`** + top-level **`summary`**). Web UI still separate.
- **Tests / smoke**: `cai-agent/tests/test_ops_gateway_skills_cli.py`; `scripts/smoke_new_features.py` exercises the three JSON paths.

### 0.6.0 (2026-04-23)

#### Breaking changes (upgrade from 0.5.x)

- **Versioned `--json` stdout**: Many commands no longer emit bare JSON arrays/strings; each payload documents **`schema_version`** and stable root keys (e.g. **`sessions_list_v1.sessions`**, **`schedule_list_v1.jobs`**). See **[`docs/schema/README.zh-CN.md`](docs/schema/README.zh-CN.md)** and **[`docs/MIGRATION_GUIDE.md`](docs/MIGRATION_GUIDE.md)**.
- **Exit codes**: `init` failures, `models ping` non-OK, and CLI dispatch fallthrough use **exit `2`** where **`1`** applied before (see schema README **S1-03**).
- **`models fetch --json`**: Wrapped as **`{"schema_version":"models_fetch_v1","models":[…]}`** (not a bare array).

#### New CLI (0.6.x train, high level)

- **In 0.6.0**: Workflow **`on_error`** / **`budget_max_tokens`**; schedule **stats / audit schema / retries / concurrency / `depends_on`**; recall **sort / `no_hit_reason` / doctor`**; **memory health / nudge / nudge-report**; versioned JSON for **sessions, commands, agents, schedule, cost budget, export, plugins**, and memory list/search/instincts (detail bullets below).
- **0.6.1–0.6.5**: **`gateway platforms`**, **`skills hub manifest`**, **`ops dashboard`**, Telegram **lifecycle / allowlist / execute-on-update / continue-hint`**.
- **0.6.6–0.6.8**: **`CAI_METRICS_JSONL`**, **`observe report`**, **`insights --json --cross-domain`**, **`observe export`**.

#### Deprecations

- **None** that remove flags without replacement in 0.6.0. **`models ping --fail-on-any-error`** remains a **no-op alias** matching the new default (exit **`2`** on any error).

- **Workflow (Hermes S5-03)**: JSON root **`on_error`**: **`fail_fast`** (default) or **`continue_on_error`** (alias **`continue-on-error`**). On fail-fast, remaining steps are **`skipped`** with **`skip_reason=fail_fast_prior_batch`** and **`workflow.step.skipped`** events; merge/conflict resolution only considers successfully finished steps when continuing. Summary adds **`on_error`**, **`steps_skipped`**, **`merge_steps_considered`**.
- **Workflow (Hermes S5-04)**: Optional root **`budget_max_tokens`**. Cumulative executed-step **`total_tokens`** gates the next batch; unstarted steps become **`skipped`/`budget_exceeded`**. **`summary`** and **`workflow.finished`** include **`budget_limit`**, **`budget_used`**, **`budget_exceeded`**; **`task.error`** may be **`workflow_budget_exceeded`**. Batches already submitted still run to completion.
- **Workflow status logic**: Fail-fast completion is detected via **`skip_reason=fail_fast_prior_batch`**, so budget-driven skips are not misclassified as fail-fast.
- **Smoke / docs**: `scripts/smoke_new_features.py` asserts **`workflow --json`** **`summary.on_error`** and **`budget_*`**; `docs/PRODUCT_PLAN.zh-CN.md` §3.0 documents the **>20%** sync baseline; `QA_SKIP_LOG=1` on `scripts/run_regression.py` skips writing new `docs/qa/runs/*.md` reports. Parity docs (`docs/schema/README.zh-CN.md`, `docs/HERMES_PARITY_*`, `docs/qa/sprint5-subagents-testplan.md`) updated for S5-03/S5-04.

### 0.5.0 – 0.5.7 (cumulative; see entries below)

- **Docs**: [`docs/PRODUCT_PLAN.zh-CN.md`](docs/PRODUCT_PLAN.zh-CN.md) **§三之二 · 3.0** adds **quantified progress** (weighted §二 items 1–26, Hermes 34 stories, T1); [`docs/schema/README.zh-CN.md`](docs/schema/README.zh-CN.md) documents **`gateway telegram` / `gateway_telegram_map_v1`** and **`schedule stats` / `schedule_stats_v1`** (S1-02; links [`SCHEDULE_STATS_JSON.zh-CN.md`](docs/schema/SCHEDULE_STATS_JSON.zh-CN.md)); archived [`docs/archive/legacy/HERMES_PARITY_PROGRESS.zh-CN.md`](docs/archive/legacy/HERMES_PARITY_PROGRESS.zh-CN.md) and [`docs/archive/legacy/DEVELOPMENT_PROGRESS_TRACKER.zh-CN.md`](docs/archive/legacy/DEVELOPMENT_PROGRESS_TRACKER.zh-CN.md) cross-link the same metrics. **§三之二** also lists dev-item counts (1–26), backlog **21–26**, and the **QA handoff checklist**.
- **`cai-agent init --json`**: Prints a single-line **`init_cli_v1`** payload (`ok`, `config_path`, `preset`, `global` on success; `error` + `message` on `config_exists` / `template_read_failed` / `mkdir_failed`). Default text output unchanged without `--json`. See `docs/schema/README.zh-CN.md`.
- **`cai-agent init` exit codes (S1-03)**: Failure paths (**`config_exists`** without `--force`, **`template_read_failed`**, **`mkdir_failed`**) now return **exit `2`** (previously **`1`**). JSON envelope unchanged (`init_cli_v1`, `ok: false`). See `docs/schema/README.zh-CN.md` breaking-changes section.
- **CLI `main()` defensive fallthrough (S1-03)**: If dispatch does not handle `args.command` (internal inconsistency), the process now returns **exit `2`** and prints one stderr line (previously **`1`** with no message). See `docs/schema/README.zh-CN.md`.
- **`cai-agent memory export --json` / `memory export-entries --json`**: Optional JSON stdout summaries **`memory_instincts_export_v1`** (`output_file`, `snapshots_exported`) and **`memory_entries_export_result_v1`** (`output_file`, `entries_count`, `export_warnings`). Default stdout (path only) unchanged. See `docs/schema/README.zh-CN.md`.
- **`cai-agent models ping` exit codes (S1-03)**: Any result with `status != OK` now returns **exit `2`** by default (previously **`1`**). **`--fail-on-any-error`** remains a **no-op alias** for backward-compatible scripts. See `docs/schema/README.zh-CN.md` breaking-changes section.
- **`cai-agent memory import` / `memory import-entries` stdout**: **`memory_instincts_import_v1`** (**`imported`**), **`memory_entries_import_result_v1`** (**`imported`**), and **`memory import-entries --dry-run`** → **`memory_entries_import_dry_run_v1`** (adds **`schema_version`** alongside existing fields). See `docs/schema/README.zh-CN.md`.
- **`cai-agent memory list|search|instincts --json` / `memory extract` stdout**: **`memory_list_v1`** (**`entries`**, `limit`, `sort`), **`memory_search_v1`** (**`hits`**, `query`, `limit`, `sort`), **`memory_instincts_list_v1`** (**`paths`**, `limit`), and **`memory_extract_v1`** (**`written`**, **`entries_appended`**). **`list`/`search`/`instincts` `--json` is a breaking change** (was a bare JSON array). See `docs/schema/README.zh-CN.md`.
- **CLI tests (empty workspace)**: Added **`test_sessions_list_json_empty_directory`** (`sessions_list_v1`), **`test_observe_report_json_empty_workspace_state_pass`** (`observe_report_v1` / `state=pass`), and **`test_memory_state_json_empty_workspace`** (`memory_state_eval_v1`).
- **`cai-agent insights --json`**: `_build_insights_payload` returns immediately when **no session files fall in the time window**, producing the same **`schema_version` `1.1`** shape as the zero-iteration loop (empty `models_top` / `tools_top`, `sessions_in_window: 0`) without scanning message bodies. Covered by **`test_insights_json_empty_workspace_fast_path`** and **`scripts/smoke_new_features.py`**.
- **`scripts/smoke_new_features.py`**: Also exercises **`mcp-check --json --list-only`** (**`mcp_check_result_v1`**, exit **`0`** or **`2`**), empty-cwd **`sessions --json`** / **`observe-report --json`**, **`hooks run-event … --dry-run --json`** (**`hooks_run_event_result_v1`**), **`memory state --json`** (**`memory_state_eval_v1`**), **`plugins --json`** (**`plugins_surface_v1`**, repo-root **`cai-agent.toml`** via **`--config`**), **`doctor --json`** (**`doctor_v1`**), **`insights --json`** (empty temp cwd), **`board --json`** (**`board_v1`** + nested **`observe`** **`1.1`**), **`hooks list --json`** (**`hooks_catalog_v1`**, minimal temp **`hooks/hooks.json`**), **`memory health --json`** (S2-01 root **`schema_version` `1.0`**), **`init --json`** (**`init_cli_v1`**), a **second `init --json`** in the same temp dir (**`config_exists`**, exit **`2`**), **`schedule add|list|rm --json`**, **`schedule stats --json`** (**`schedule_stats_v1`**), **`gateway telegram list --json`** (**`gateway_telegram_map_v1`**), **`recall --json`** (**`schema_version` `1.3`**, **`no_hit_reason`** on empty workspaces), and **`memory`** JSON paths; it shells **`python -m cai_agent`** with **`cai-agent/src`** on **`PYTHONPATH`** (same as **`run_regression.py`**, no reliance on a **`cai-agent`** script on **`PATH`**).
- **`cai-agent schedule add|list|rm|add-memory-nudge --json`**: Adds **`schema_version`** payloads (**`schedule_add_v1`**, **`schedule_list_v1` + `jobs`**, **`schedule_rm_v1`**, **`schedule_add_memory_nudge_v1`**, and **`schedule_add_invalid_v1`** on validation failure). **`schedule list --json` is a breaking change** (was a bare array). See `docs/schema/README.zh-CN.md`.
- **`cai-agent schedule run-due --json` / `schedule daemon --json`**: Stdout payloads now include **`schema_version`: `schedule_run_due_v1`** / **`schedule_daemon_summary_v1`** (including the daemon lock-conflict JSON). See `docs/schema/README.zh-CN.md`.
- **`cai-agent cost budget`**: The always-JSON stdout payload now includes **`schema_version`: `cost_budget_v1`** (alongside `state` / `total_tokens` / `max_tokens`; still no `--json` flag). See `docs/schema/README.zh-CN.md`.
- **`cai-agent sessions --json`**: Output is now a versioned object **`sessions_list_v1`** with **`sessions`** (plus **`pattern`**, **`limit`**, **`details`**) (**breaking change**: previously a bare JSON array). See `docs/schema/README.zh-CN.md`.
- **`cai-agent commands --json` / `agents --json`**: Output is now versioned objects **`commands_list_v1`** / **`agents_list_v1`** with **`commands`** / **`agents`** arrays (**breaking change**: previously bare JSON string arrays). See `docs/schema/README.zh-CN.md`.
- **`cai-agent export` JSON line**: Adds **`schema_version`: `export_cli_v1`** to the stdout payload from `export_target` (all `--target` modes).
- **`cai-agent plugins --json`**: Payload now includes **`schema_version`: `plugins_surface_v1`** alongside the existing **`plugin_version`** (`list_plugin_surface`).
- **`cai-agent models fetch --json` contract**: Output is now **`{"schema_version":"models_fetch_v1","models":[…]}`** (**breaking change**: previously a bare JSON string array; automation should read **`models`**). See `docs/schema/README.zh-CN.md`.
- **Schedule stats SLA rollup (Hermes S4-05)**: Added **`cai-agent schedule stats`** with **`--json`**, **`--days`** (1–366, default 30), and optional **`--audit-file`**. Output **`schema_version`: `schedule_stats_v1`** and per-task **`success_rate`**, **`avg_elapsed_ms`**, **`p95_elapsed_ms`**, **`run_count`**, **`fail_count`** derived from **`task.completed` / `task.failed` / `task.retrying`** lines in `.cai-schedule-audit.jsonl` (legacy rows without `event` are mapped). See `docs/schema/SCHEDULE_STATS_JSON.zh-CN.md`.
- **Schedule audit JSONL schema (Hermes S4-04)**: `.cai-schedule-audit.jsonl` and `schedule daemon --jsonl-log` now emit **one consistent JSON shape** per line: `schema_version` (**`1.0`**), `event` (`task.started` / `task.completed` / `task.failed` / `task.retrying` / `task.skipped` / `daemon.cycle` / `daemon.started`), plus `task_id`, `goal_preview`, `elapsed_ms`, `error`, `status`, `action`, and `details`. `schedule run-due --execute` logs **`task.started`** before invoking the agent. See `docs/schema/SCHEDULE_AUDIT_JSONL.zh-CN.md`.
- **Schedule dependency validation & list view (Hermes S4-03)**: `add_schedule_task` rejects **`depends_on` cycles** (including self-edges) before persisting. `cai-agent schedule add` surfaces failures as **exit 2** with JSON `{"ok": false, "error": "schedule_add_invalid", "message": ...}`. `cai-agent schedule list` enriches output with **`depends_on_status`**, **`dependency_blocked`**, **`dependents`**, and a readable **`depends_on_chain`** (list JSON only; not stored in `.cai-schedule.json`). Text list adds compact `deps` / `dep_blocked` / `dependents` / `dep_chain` columns.
- **Schedule daemon concurrency cap (Hermes S4-02)**: `cai-agent schedule daemon` adds **`--max-concurrent`** (default **1**; **0** is treated as **1**). Each poll cycle executes at most N due jobs; additional due jobs are skipped for that cycle (not marked failed) and recorded as **`skipped_due_to_concurrency`** in `.cai-schedule-audit.jsonl` and in optional `--jsonl-log` lines. Final JSON adds `max_concurrent`, `total_skipped_due_to_concurrency`, and per-cycle `skipped_due_to_concurrency` / `skipped_due_to_concurrency_count`.
- **Schedule cross-run retries (Hermes S4-01)**: `.cai-schedule.json` tasks now persist `max_retries` (default **3**, CLI `schedule add --max-retries`), `retry_count`, and `next_retry_at`. After a failed `run-due --execute` / `daemon --execute`, `last_status` becomes **`retrying`** until the next backoff window elapses (`60 * 2^(retry_count-1)` seconds), then **`failed_exhausted`** when retries are exhausted; success clears counters. `compute_due_tasks` only surfaces `retrying` jobs when `next_retry_at` is due. JSON / audit lines for failures mirror the persisted status and retry fields.
- **Recall sort modes (Hermes S3-01)**: `cai-agent recall --json` and `recall-index search|benchmark` now accept `--sort recent|density|combined` (default `recent`). Payload includes `sort` plus an explicit `ranking` description; keyword-density scoring uses full matched message bodies (not only snippets) for more stable density ordering.
- **Recall no-hit reasons (Hermes S3-02)**: When a recall query returns zero hits, JSON payloads now include **`no_hit_reason`** (`window_too_narrow` / `pattern_no_match` / `index_empty` / `all_skipped`) and bump **`schema_version` to `1.3`**. Text mode prints a short human-readable hint line. Index search uses full indexed `content` for density scoring.
- **Recall index doctor (Hermes S3-03)**: Added `cai-agent recall-index doctor [--fix] [--json]` with **`schema_version: recall_index_doctor_v1`**, reporting `is_healthy`, `issues`, `stale_paths`, `missing_files`, and `schema_version_ok`. **`--fix`** rewrites the index after pruning missing/stale/invalid rows, then re-checks. Exit **0** when healthy, **2** when problems remain (including missing index file or bad JSON).
- **Recall perf bench (Hermes S3-04)**: Added `scripts/perf_recall_bench.py` to generate synthetic sessions and print a Markdown table with median **scan / index_build / index_search** timings (optional `--include-refresh` for no-op refresh timing). Defaults to writing under `docs/qa/runs/`; includes reference threshold columns for 200-session runs.
- **Memory health (Hermes parity)**: Added `cai-agent memory health --json` with `schema_version=1.0`, composite `health_score` / `grade` (A–D), and sub-metrics `freshness`, `coverage`, `conflict_rate` plus structured observability (`conflict_pairs`, `conflict_pair_count`, `conflict_compared_entries`, `conflict_max_compare_entries`, `conflict_similarity_metric`, coverage denominator helpers under `counts`). CLI flags include `--days`, `--freshness-days`, `--session-pattern`, `--session-limit`, `--conflict-threshold`, `--max-conflict-compare-entries`, and `--fail-on-grade` (exit 2 when the grade is not strictly better than the threshold).
- **Memory nudge recommendations**: Added `cai-agent memory nudge` (Hermes-inspired memory hygiene loop) to evaluate recent session volume and current memory health, then emit actionable reminders. Supports `--days`, `--session-pattern`, `--session-limit`, and `--json` output (`schema_version=1.0`, `severity`, `actions`, `memory_warnings`, `latest_instinct_path`) for automation and scheduled checks. Added `--write-file` to persist the JSON payload for cron/CI handoff, plus `--fail-on-severity medium|high` to return non-zero when severity reaches a configured threshold. When `--write-file` is set, the same JSON line is appended to `memory/nudge-history.jsonl` by default (override with `--history-file`; deduped when paths match).
- **Memory nudge trend report**: Added `cai-agent memory nudge-report` to aggregate `memory/nudge-history.jsonl` into an operator-friendly trend snapshot (`schema_version=1.2`, `history_total`, `severity_counts`, `latest_severity`, `severity_trend`, `severity_jumps`, `avg_recent_sessions`, `avg_memory_entries`, plus `health_score` / `health_grade` / `freshness` aligned with `memory health`). Supports `--history-file`, `--days` (time window filter), `--freshness-days`, `--limit`, and `--json`; useful for QA/ops drift monitoring across runs. `memory nudge --write-file` appends the same JSON line to the default history file (or `--history-file`) unless it points at the same path as `--write-file`.
- **Schedule nudge template shortcut**: Added `cai-agent schedule add-memory-nudge` to create a standard daily memory-governance task in one command. It generates a canonical nudge goal (JSON output + write-file + severity gate), supports overrides (`--every-minutes`, `--output-file`, `--fail-on-severity`, `--workspace`, `--model`, `--disabled`), and reduces manual quoting/flag drift when integrating with schedule daemon.
- **Recall index incremental refresh**: `recall-index refresh` merges into `.cai-recall-index.json` (schema `1.1`): skips re-parsing sessions whose `mtime` is unchanged, preserves entries outside the current `--days` scan window, and optional `--prune` removes missing files or entries older than the window. `recall-index build` remains a full rebuild. `recall --use-index` reads the index via `--index-path`; `recall-index` subcommands use `--index-path` consistently (`dest=index_path`).
- **Cross-session recall search**: Added `cai-agent recall` for Hermes-style session recall with keyword/regex search over saved session JSON files. Supports `--query`, `--regex`, `--days`, `--pattern`, `--limit`, `--json`, and returns ranked snippets with file path, mtime, model, and answer preview.
- **Schedule daemon guardrails**: `cai-agent schedule daemon` now supports a single-instance lock (`.cai-schedule.lock`) to prevent duplicate runners in one workspace, and optional JSONL append logging via `--log-file`. Added startup metadata (`pid`, `started_at`, mode flags) and per-cycle log records suitable for QA/ops tracing.
- **Schedule execute mode**: `cai-agent schedule run-due --execute` now performs real agent runs for due tasks (instead of metadata-only marking), captures per-task answer/error/elapsed metrics, and persists run status (`last_run_at`, `last_status`, `last_error`, `run_count`) back into `.cai-schedule.json`. `schedule list` includes `last_status`/`run_count` in text output; `schedule add --disabled` remains supported.
- **Zhipu AI (BigModel) OpenAI-compatible routing**: `profiles.PRESETS` adds **`zhipu`** (`cai-agent models add --preset zhipu …`); `normalize_openai_chat_base_url` / `project_base_url` **do not append `/v1`** to `https://open.bigmodel.cn/api/paas/v4` so chat hits `…/chat/completions` per [Zhipu’s OpenAI-compat guide](https://docs.bigmodel.cn/cn/guide/develop/openai/introduction). Example templates document **`ZAI_API_KEY`** + **`glm-5.1`**.
- **HTTP proxy vs local LLM**: When `[llm].http_trust_env` is `true`, httpx still uses **`trust_env=false` for loopback** (`localhost`, `127.*`, `::1`) on OpenAI-compat **chat**, **`GET …/models`** / `ping_profile`, and **MCP** HTTP clients so corporate proxies cannot break local LM Studio/Ollama with bogus **503** responses.
- **Sprint 3 — TUI model panel (M4)**: `Ctrl+M` / `/models` opens a richer panel (columns `id | model | provider | base_url | notes | [active]`; `Enter` switches chat profile; `a`/`e`/`d`/`t` add/edit/delete/ping with TOML persistence consistent with `cai-agent models`; empty-state guidance when the list is empty). See `docs/MODEL_SWITCHER_DEVPLAN.zh-CN.md` §4.
- **Sprint 3 — `/use-model` + provider hint (M5)**: When switching profiles changes the **provider**, the TUI appends a short hint suggesting `/compact` or `/clear` if context looks wrong across vendors.
- **Sprint 3 — `/status` + session fields (M7)**: `/status` prints an explicit `profile:` line; TUI `/save` and `run --save-session` JSON now include **`profile`** (active profile id) alongside **`active_profile_id` / `subagent_profile_id` / `planner_profile_id`**; session load accepts **`profile`** as a fallback for the active id.
- **Sprint 3 — docs / parity (M9) + P1 decision**: `docs/PARITY_MATRIX.zh-CN.md` adds a `Done` row for multi-profile + TUI; new `docs/WEBSEARCH_NOTEBOOK_MCP.zh-CN.md` records the **MCP-first** decision for WebSearch/Notebook and how `board --json` relates to `observe`; `board --json` adds top-level **`observe_schema_version`** mirroring the embedded `observe` payload.
- **Init & model presets for local stacks and gateways**: `cai-agent init --preset starter` copies a bundled `cai-agent.starter.toml` with explicit profiles for LM Studio, Ollama, vLLM, OpenRouter, **Zhipu GLM**, and a placeholder self-hosted OpenAI-compatible gateway. `cai-agent models add --preset` now includes **`vllm`**, **`gateway`**, and **`zhipu`** (plus existing presets); `profiles.PRESETS` documents default ports (`8000` / `8080`) and env vars (`VLLM_API_KEY`, `OPENAI_API_KEY`, `ZAI_API_KEY`). README / README.zh-CN / `docs/ONBOARDING.zh-CN.md` describe the flow; `doctor` hints at `init --preset starter` and the new CLI presets.
- **Removed Cursor Cloud API integration**: Dropped `cai-agent cursor`, `cai_agent.cursor_cloud`, TOML `[cursor]` / `CURSOR_*` settings, the TUI `Ctrl+Shift+M` / `/cursor-launch` flow, and the extra `doctor` line. **`cai-agent export --target cursor` is unchanged** — it only copies `rules/` / `skills/` / etc. into `.cursor/cai-agent-export` for IDE portability, not the Cloud Agents HTTP API.
- **TUI text selection & copy**: Dragging the mouse over the chat area now produces a real selection (`ALLOW_SELECT=True` set explicitly on the app so a future default flip won't regress us), and the new `Ctrl+Shift+C` binding calls `Screen.get_selected_text()` + `App.copy_to_clipboard()` to push it to the system clipboard (with a toast showing the copied character count, or a helpful hint when nothing is selected). `Ctrl+Shift+A` selects the entire chat area via `RichLog.text_select_all()` for one-shot copy. `Ctrl+C` stays bound to stop-run per terminal convention; the welcome banner and `/help` advertise the new shortcuts and the Windows-Terminal `Shift+drag` fallback.
- **Config discovery: user-level global fallback**: When `--config` / `CAI_CONFIG` / cwd-walk / `CAI_WORKSPACE` / `-w` hint all fail, `_resolve_config_file` now also tries a user-level config path, so `cai-agent ui` from any directory finds your settings. Order: `%APPDATA%\cai-agent\cai-agent.toml` (Windows) → `$XDG_CONFIG_HOME/cai-agent/cai-agent.toml` (default `~/.config/cai-agent/cai-agent.toml`) → `~/.cai-agent.toml` → `~/cai-agent.toml`. New `cai-agent init --global` seeds the OS-appropriate location (creating parent dirs as needed) from the bundled template. Project-level TOML still wins over the global one; `CAI_CONTEXT_WINDOW` env and per-profile `context_window` still override `[llm].context_window`. The "source=default" welcome hint now explicitly tells users about `init --global`, `CAI_CONFIG`, `--config`, and `-w` as the four concrete escape hatches.
- **Config discovery: workspace hint**: `Settings.from_env` / `from_sources` accept a new `workspace_hint` parameter (wired from every `cai-agent <cmd> -w/--workspace` call site in `__main__.py`). After cwd-walk, the resolver also walks from `CAI_WORKSPACE` and `workspace_hint`, fixing the "`cd` elsewhere, `-w` back to the project, and still read 8192" case.
- **TUI context usage bar**: A new row above the input shows `ctx ███░░ prompt_tokens / context_window (pct%)` — green <70%, yellow 70–89%, red ≥90%. Values come from the server's `prompt_tokens` after each response; before the first call the bar falls back to a **CJK-aware estimator** (`~N`, "估算" — CJK ~1.5 chars/token, ASCII ~4 chars/token so Chinese-heavy prompts aren't under-counted by 2–3×). Configurable via `[llm].context_window` (fallback), `[[models.profile]].context_window` (preferred), or `CAI_CONTEXT_WINDOW`. Default `8192`. `Settings` gains `context_window_source` (`profile|llm|env|default`) and both the TUI welcome banner and `/status` now print the resolved window and its source so users can diagnose "why is my denominator 8k" at a glance. Pressing Enter re-estimates immediately (doesn't wait for the server round-trip). Also exposes `cai_agent.llm.get_last_usage()` / `estimate_tokens_from_messages()` for programmatic use; `graph.llm_node` emits a `phase="usage"` progress event after every LLM call.
- **Empty-completion guard** (Qwen3 / DeepSeek-R1 / LM Studio reasoning models): when the server returns `content=""` plus a large `reasoning_content` (reasoning-budget exhaustion), the OpenAI-compat and Anthropic adapters now synthesise a `{"type":"finish","message":"[empty-completion] ..."}` envelope with an actionable diagnostic instead of crashing on `extract_json_object("")` or spinning to `max_iterations`. `<think>…</think>` prefixes are also stripped transparently. Two old Anthropic "raise on empty" contracts flipped to the new envelope — see `test_llm_empty_content_guard.py`.
- **`plan --json` + missing config**: `--config` pointing at a non-existent file now prints a JSON error (`error: config_not_found`) instead of only stderr.
- **`stats` (text mode)**: One-line summary adds `run_events_total`, `sessions_with_events`, and `parse_skipped`.
- **Hooks**: `observe_start` / `observe_end` wrap `observe`; `cost_budget_start` / `cost_budget_end` wrap `cost budget`; human `observe` line includes `run_events_total`.
- **`stats --json`**: Adds `stats_schema_version` (`1.0`), `run_events_total`, `sessions_with_events`, `parse_skipped`, and `session_summaries` (per-file `events_count`, `task_id`, tokens, tool stats).
- **`plan --json` errors**: Empty goal or LLM failure returns a JSON line with `ok: false`, `error` (`goal_empty` / `llm_error`), and `task` marked `failed` when applicable; success payloads include `ok: true`.
- **Hooks**: `memory_start` / `memory_end` wrap `cai-agent memory`; `export_start` / `export_end` wrap `cai-agent export` (adds `-w` / `--workspace` on export).
- **`plan --json`**: Stable envelope with `plan_schema_version` (`1.0`), `generated_at` (UTC ISO), and `task` (`plan-*` id via `new_task`).
- **`sessions --json`**: Without `--details`, each row still tries to parse the session file and adds `events_count`, `run_schema_version`, `task_id`, `total_tokens`, and `error_count` when possible (`parse_error` on failure). `--details` text lines now include `events=…`.
- **`security-scan` hooks**: `security_scan_start` / `security_scan_end` wrap `cai-agent security-scan` (stderr hook id listing; `security_scan_end` still runs if the scan raises).
- **Session files**: `--save-session` now persists `run_schema_version`, `events`, tool stats (`tool_calls_count`, `used_tools`, `last_tool`, `error_count`), and `post_gate` when applicable—aligned with `run --json`.
- **observe**: Per-session rows include `task_id`, `events_count`, and `run_schema_version`; aggregates add `run_events_total` and `sessions_with_events`.
- **workflow hooks**: `workflow_start` / `workflow_end` hook events (stderr hook id listing, same as session hooks) wrap `cai-agent workflow`.
- **quality-gate hooks**: `quality_gate_start` / `quality_gate_end` wrap the standalone `cai-agent quality-gate` command; `quality-gate` also honors `-w` / `--workspace` when passed via the shared parser.
- **fetch_url**: Block common SSRF hostnames (`localhost`, GCP metadata hosts) before allowlist checks.
- **fetch_url tool**: Opt-in HTTPS GET with host allowlist, size cap, and timeout; gated by `[fetch_url]` and `[permissions].fetch_url` (default deny). See `templates/cai-agent.example.toml` and `docs/MCP_WEB_RECIPE.zh-CN.md`.
- **Run JSON envelope**: `run --json` / `continue --json` (and shared path for `command` / `agent` / `fix-build`) include `run_schema_version` and `events` (`run.started` / `run.finished`) aligned with `workflow` telemetry style.
- **Memory entries**: Validate each `memory/entries.jsonl` row before append (v1 shape; JSON Schema file under `cai_agent/schemas/` for external tooling).
- **Doctor**: Prints `fetch_url` enablement and allowlist count when enabled.
- **QA regression logs**: `scripts/run_regression.py` writes a timestamped Markdown report under `docs/qa/runs/` (see `docs/QA_REGRESSION_LOGGING.md`); CI uploads those files as workflow artifacts.
- **Changelog split**: `CHANGELOG.md` is now English by default; the previous Chinese text lives in `CHANGELOG.zh-CN.md`.
- **Readme split**: `README.md` is English by default; the previous Chinese readme is in `README.zh-CN.md`, with cross-links at the top of each file.
- **JSON diagnostics**: `run --json` / `continue --json` add `last_tool` and `error_count`.
- **Session management**: New `cai-agent sessions` subcommand; TUI adds `/sessions` and `/load latest` for quick restore.
- **Session details**: `cai-agent sessions --details` shows message count, tool calls, error count, and answer preview per file.
- **Session matching fix**: `sessions` and `/load latest` default to `.cai-session*.json`, including `.cai-session.json` and auto-named files.
- **Rules expansion**: New common and Python rule docs (naming/structure, logging/errors, secrets, Git, docs, performance, context/memory, MCP, typing, tests/CI, packaging, CLI/TUI, config evolution).
- **Skills expansion**: New workflow docs (planning, refactors, features+tests, debugging, security scan, performance, deps, API integration, reviews, pre-release, workflows, migrations).
- **Readme sync**: Describes `rules/` and `skills/` as a reusable content library beyond skeleton dirs.
- **Rules round 2**: Hooks, sub-agents, validation, research-first, prompt hygiene, concurrency, HTTP retries.
- **Skills round 2**: search-first, TDD, verification loop, hook design, sub-agent orchestration, memory extraction, coverage audit, hardening, postmortems, doc sync.
- **Readme sync 2**: Governance and execution workflow themes.
- **Runtime skeleton**: `commands/`, `agents/`, `hooks/` with `hooks.json` and session lifecycle guidance.
- **Readme sync 3**: Documents commands/agents/hooks and “library → runtime surface” positioning.
- **Bugfix (tool node)**: `graph` tool node avoids raw `pending` indexing; uses validated `name`/`args` to reduce KeyError risk.
- **CLI command templates**: `cai-agent commands` and `cai-agent command <name> <goal...>` read `commands/*.md` as templates.
- **Hook runtime**: `hook_runtime` reads `hooks/hooks.json` on session start/end for `run`/`continue`/`command` (non-JSON mode prints enabled hook ids).
- **Readme sync 4**: Usage for `commands`/`command` and hook runtime.
- **Bugfix (command save_session)**: `command` subcommand gains safe `save_session` handling.
- **CLI agents**: `cai-agent agents` and `cai-agent agent <name> <goal...>` load `agents/*.md` role templates.
- **Auto skill injection**: `command` / `agent` injects matching `skills/*.md` (same name or prefix).
- **Readme sync 5**: Documents `agent` usage and command/role + skill composition.

### 0.4.1

- **TUI save**: `/save` may omit path; default filename `.cai-session-YYYYMMDD-HHMMSS.json`.

### 0.4.0

- **JSON fields**: `run --json` / `continue --json` add `tool_calls_count` and `used_tools`.
- **TUI load summary**: After `/load <path>`, show assistant rounds, tool calls, last answer preview.

### 0.3.9

- **JSON fields**: `run --json` / `continue --json` add `provider`, `model`, `mcp_enabled`, `elapsed_ms` for scripts/CI.
- **TUI sessions**: `/save <path>` and `/load <path>` in the UI.

### 0.3.8

- **Readme at repo root**: Single top-level readme to avoid duplicating `cai-agent/README.md`.
- **MCP probe**: `mcp-check` adds `--tool` / `--args` for a real tool call after listing tools.

### 0.3.7

- **Cross-platform docs**: macOS/Linux install, config copy, env vars, common commands.
- **MCP ops**: `mcp-check` adds `--force` / `--verbose`; TUI adds `/mcp refresh` and `/mcp call <name> <json_args>`.

### 0.3.6

- **MCP usability**: `cai-agent mcp-check`; `mcp_list_tools` short TTL cache (15s, `force=true` refresh); TUI `/mcp`.

### 0.3.5

- **MCP Bridge**: `mcp_list_tools` / `mcp_call_tool` with config; `doctor` and TUI `/status` show MCP state.

### 0.3.4

- **Git read-only tools**: `git_status` and `git_diff` to scope changes before heavy file reads.

### 0.3.3

- **TUI model management**: `/models` and `/use-model <id>` to list and switch models without restart.

### 0.3.2

- **Copilot model pick**: `cai-agent models` (`/v1/models`) and global `--model` for `run`/`continue`/`ui`/`doctor`/`models`.

### 0.3.1

- **Copilot provider**: `llm.provider` (`openai_compatible` / `copilot`); `doctor` and `/status` show provider; `[copilot]` and `COPILOT_*` env vars.

### 0.3.0

- **Session I/O**: `run` supports `--save-session` / `--load-session` for JSON `messages` round-trip.
- **`cai-agent continue`**: Continue from session JSON (same idea as `run --load-session`).
- **`run_command`**: `cwd` relative to workspace, still sandboxed.

### 0.2.x

- **`cai-agent doctor`**: Resolved config, workspace, doc files, git repo flag; **redacts API key**; `--config`, `-w` / `--workspace`.
- **`Settings.config_loaded_from`**: Absolute path of loaded TOML (or `None`); included in `run --json` for scripts.
- **`run --json`**: One-line JSON to stdout (`answer`, `iteration`, `finished`, `config`, `workspace`); no stderr transcript spam.
- **LLM retries**: Backoff on HTTP **429 / 502 / 503 / 504** (up to 5 attempts).
- **Tools**: `read_file` **`line_start` / `line_end`** (optional read to EOF); **`list_tree`** with depth/entry limits.
- **TUI**: **`/status`** model/workspace; **`/reload`** rebuilds first system message (project + git context).

### 0.1.x and earlier (summary)

- **`cai-agent init`**: Writes `cai-agent.toml` (`--force` overwrite).
- **Config**: `temperature`, `timeout_sec`, `project_context`, `git_context`, etc.; env overrides.
- **System prompt**: Optional `CAI.md` / `AGENTS.md` / `CLAUDE.md` and read-only git summary.
- **Tools**: `glob_search`, `search_text`; `run` / `ui`; bundled example TOML template.
