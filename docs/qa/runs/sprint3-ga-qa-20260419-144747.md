# Sprint 3 GA 前 QA 回归报告（devplan §4.2 + §5）

> 本文由 QA 在 **GA tag 前**产出，依据 [`docs/MODEL_SWITCHER_DEVPLAN.zh-CN.md`](../../MODEL_SWITCHER_DEVPLAN.zh-CN.md) **§4.2 Sprint 3 DoD（前五条技术项）** 与 **§5 QA 计划**（环境 §5.1、矩阵 §5.2、缺陷等级 §5.3、产出 §5.4）。**打 `GA` tag 与内测公告**由 PM 执行，不在本报告 Go 范围内。
>
> 报告体例对齐：[sprint2-qa-20260418-035002.md](sprint2-qa-20260418-035002.md)。

## 0. 基本信息

| 项 | 内容 |
|----|------|
| **Sprint** | S3（TUI 体验 + 发版收尾）— **GA 前冻结验证** |
| **分支 / HEAD** | 当前验证：`6c0454682e3f48b9d7c9c800c68b803c51353181` |
| **生成时间** | 2026-04-19 14:47:47（UTC+8 本机，与本轮 `run_regression` 结束时间一致） |
| **Python / 平台** | 3.13.13 / Windows 11 (10.0.26200) |
| **§5.1 环境** | 本机：`pip install -e cai-agent` 已就绪；无强制真实 `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`（自动化以 mock / 本地路径为主，符合 §5.1「可选」说明） |

## 1. 自动化基线（§4.2 第 5 条 + GA 前全量要求）

| 项 | 命令 | 结果 | 备注 |
|----|------|------|------|
| **pytest 全量** | `python -m pytest -q cai-agent/tests` | **188 passed** | 1.14 s；含 `test_tui_model_panel.py`、`test_context_usage_bar.py`、`test_factory_routing_and_security.py` 等 |
| **脚本回归** | `python scripts/run_regression.py` | **PASS（27/27）** | 报告：[regression-20260419-144734.md](regression-20260419-144734.md) |

`security-scan --json`（回归步骤内）对当前仓 **exit=0**；`mcp-check` **exit=2**（MCP 未启用，预期内）。

## 2. Sprint 3 五条测点（映射 §4.2 DoD 前五项，不含打 GA tag）

| # | DoD 原文（§4.2） | 验证方式 | 结果 | 证据 / 说明 |
|---|------------------|----------|------|----------------|
| **1** | TUI `/models` 面板完成所有子动作（add/edit/rm/ping/switch） | 代码审查 + pytest 子集 + 手工计划 | **PASS（自动化）** / **建议手工抽检** | 实现：`cai_agent/tui.py` 挂载 `ModelPanelScreen`（`Ctrl+M` / `/models`）；`CHANGELOG.md` §0.5.0 首条用户说明列明 `Enter` / `a`/`e`/`d`/`t` 与空态。单测：`test_tui_model_panel.py`（行渲染、`_next_active_id` 回退）。**完整 Textual 五键路径**仍以 [`docs/qa/s3-tui-model-panel-testplan.md`](../s3-tui-model-panel-testplan.md) 为冻结日前一天主回归清单。 |
| **2** | `/status` 显示 profile + 路由；session 文件含 `profile` | 代码审查 + 全量 pytest | **PASS** | `tui.py`：`/status` 与 `/save` payload 含 `profile`（约 L1137）；`__main__.py`：`run --save-session` 路径写入 `"profile": settings.active_profile_id`（约 L2553）；会话加载识别 `profile` 别名（`tui.py` 约 L587）。 |
| **3** | [`PARITY_MATRIX.zh-CN.md`](../../PARITY_MATRIX.zh-CN.md) 增一行 Done | 文档审查 | **PASS** | 矩阵表含行：`多模型 profile / TUI 面板 / /use-model / session profile 字段` → **`Done`**（与 devplan §4 交叉引用一致）。 |
| **4** | `CHANGELOG` 4 条用户语言（backlog §10 / PM 审阅） | 文档审查 | **PASS** | `CHANGELOG.md` **L7–L10** 四条 Sprint 3 用户向条目（M4 面板、M5 `/use-model`+提示、M7 `/status`+session、`PARITY`+M9 文档）；中文镜像见 `CHANGELOG.zh-CN.md`。 |
| **5** | **全量回归**：`python scripts/run_regression.py` 绿 | 脚本执行 | **PASS** | 见 §1；产物 `regression-20260419-144734.md`。 |

> **§4.2 第六条**（`GA` tag 发布；内测公告 §6.3）— **PM 动作**，QA 本报告不判 Go/No-Go。

## 3. devplan §5.2 回归矩阵（与本 Sprint 直接相关行）

| 场景 | Sprint | 动作 | 本轮结果 | 证据 |
|------|--------|------|----------|------|
| TUI 面板 | **S3** | 五个子动作全走一遍 | **部分自动化 + 计划内手工** | 见 §2 测点 **#1**；手工矩阵见 [`s3-tui-model-panel-testplan.md`](../s3-tui-model-panel-testplan.md) |
| 全量回归脚本 | **S3** | `python scripts/run_regression.py` 绿 | **PASS** | [regression-20260419-144734.md](regression-20260419-144734.md) |

其余 §5.2 行（老 `[llm]`、`models` CLI、`doctor`、S2 供应商矩阵、`security-scan` 等）由 **pytest 全量 + 回归脚本** 一并覆盖，本轮无失败步骤。

## 4. 安全 / 非功能（§5.3 缺陷等级语境下抽查）

| 项 | 结果 | 说明 |
|----|------|------|
| `security-scan` 新规则（明文 key） | PASS | `test_factory_routing_and_security::SecurityScanProfileRuleTests` 在 188 条内；回归步骤 `security-scan --json` 通过 |
| `models` TOML 原子写入 / `.bak` | PASS | `test_model_profiles_cli::test_atomic_write_creates_backup` 等仍在绿集内 |
| 密钥不落明文到 session / doctor 打码 | PASS | 延续 S2 结论；S3 增补 `profile` 字段不含 key 值（代码路径审查，见 §2 测点 **#2**） |

## 5. 已知限制（不阻断本报告范围内的 GA 前技术 Go）

1. **TUI 面板完整手工矩阵**：未在本机本轮逐条执行（耗时）；冻结日前一天按 `s3-tui-model-panel-testplan.md` 全量执行并另出 `docs/qa/runs/s3-tui-YYYYMMDD-HHmmss.md`。
2. **真实供应商 E2E**（`OPENAI_API_KEY` / `ANTHROPIC_API_KEY` 下 `run "hello"`）：仍属 §5.1 可选；内测按 §6.3 自检。

## 6. 缺陷（§5.3）

- 本轮自动化与文档核对：**0 个 P0 / P1** 自报。
- 未登记 P2（若手工矩阵发现 UI 文案问题，单开 Issue）。

## 7. Go / No-Go（GA 前技术项）

**Go — Sprint 3 §4.2 前五条技术 DoD 已由自动化 + 文档核对满足；建议 PM 在手工矩阵抽检无 P0 后打 `GA` tag 并发 §6.3 公告。**

理由摘要：

- **188/188** pytest + **27/27** `run_regression.py` 全绿；
- **PARITY_MATRIX** 对应行已为 `Done`；
- **CHANGELOG** 四条 Sprint 3 用户语言已就位；
- **`profile` + `/status` + session** 与代码路径一致；
- TUI 五子动作：**实现与部分单测已绿**；**完整 Textual 冒烟**依赖计划内手工报告兜底（§5.2「TUI 面板」行语义）。

---

*本报告维护者：QA；关联：[MODEL_SWITCHER_BACKLOG.zh-CN.md](../../MODEL_SWITCHER_BACKLOG.zh-CN.md)、[MODEL_SWITCHER_DEVPLAN.zh-CN.md](../../MODEL_SWITCHER_DEVPLAN.zh-CN.md)、[QA_REGRESSION_LOGGING.md](../../QA_REGRESSION_LOGGING.md)。*
